{
  "name": "celestia-ai",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "check:strictness": "node scripts/check-core-strictness.mjs",
    "check:all": "pnpm run check:strictness && pnpm run typecheck",
    "supabase": "supabase"
  },
  "devDependencies": {
    "supabase": "^2.92.1",
    "turbo": "^2.8.1",
    "typescript": "^5.7.3"
  },
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=22.0.0"
  }
}
