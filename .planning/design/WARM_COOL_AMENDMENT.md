# Continuity-layer amendment — warm/cool zoning + source-quality mapping

**Retired 2026-08-16 — superseded by `.planning/design/DESIGN-LANGUAGE-REFERENCE.md` as the sole current design-language reference.**

This document was the original bronze/cool token proposal. Everything in it that turned out correct is now in `DESIGN-LANGUAGE-REFERENCE.md` §1–§2, sourced from what actually shipped rather than the proposal. Its `bronzeText` value (`#e0b587`) is the one thing it got wrong — the committed mockups (`.planning/design/mockups/*.html`, `--bronze-hi`) use `#d9a06a`, which is what shipped and is now canonical (Batch 6, 2026-08-16).

Original content (the audit, the R4 re-amendment, the file-by-file amber migration scope) is preserved in git history if historical context is ever needed — see `git log -- .planning/design/WARM_COOL_AMENDMENT.md`.
