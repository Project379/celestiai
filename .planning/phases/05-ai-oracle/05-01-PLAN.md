---
phase: 05-ai-oracle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/schema/users.ts
  - packages/db/src/schema/ai-readings.ts
  - packages/db/src/schema/index.ts
  - apps/web/lib/oracle/prompts.ts
  - apps/web/lib/oracle/chart-to-prompt.ts
  - apps/web/lib/oracle/planet-parser.ts
autonomous: true
user_setup:
  - service: google-ai
    why: "Gemini API for AI-generated readings"
    env_vars:
      - name: GOOGLE_GENERATIVE_AI_API_KEY
        source: "Google AI Studio (https://aistudio.google.com/apikey) -> Create API key"
    dashboard_config: []

must_haves:
  truths:
    - "users table exists with clerk_id and subscription_tier columns"
    - "ai_readings table exists with chart_id, topic, content, expires_at columns"
    - "Prompt builder produces Bulgarian mystical-guide system prompt with sentinel markers"
    - "Chart data can be serialized to textual prompt format"
    - "Planet sentinel markers can be parsed and stripped from text"
  artifacts:
    - path: "packages/db/src/schema/users.ts"
      provides: "users table with subscription_tier"
      contains: "subscriptionTier"
    - path: "packages/db/src/schema/ai-readings.ts"
      provides: "ai_readings table with expiry and regeneration tracking"
      contains: "aiReadings"
    - path: "apps/web/lib/oracle/prompts.ts"
      provides: "System prompt builder with topic suffixes"
      exports: ["buildSystemPrompt"]
    - path: "apps/web/lib/oracle/chart-to-prompt.ts"
      provides: "Chart data to textual prompt serializer"
      exports: ["chartToPromptText"]
    - path: "apps/web/lib/oracle/planet-parser.ts"
      provides: "Sentinel parser and stripper"
      exports: ["extractPlanetMentions", "stripSentinels"]
  key_links:
    - from: "packages/db/src/schema/ai-readings.ts"
      to: "packages/db/src/schema/charts.ts"
      via: "Foreign key on chart_id"
      pattern: "references.*charts\\.id"
    - from: "packages/db/src/schema/index.ts"
      to: "packages/db/src/schema/users.ts"
      via: "Re-export"
      pattern: "export.*users"
---

<objective>
Create database schema for users and AI readings, plus the Oracle prompt engineering utilities.

Purpose: Establish the data layer (users table for subscription tier, ai_readings table for cached readings) and the prompt generation utilities (system prompt builder, chart-to-prompt serializer, sentinel parser) that the streaming API route and UI will consume.

Output: Two new database tables with migrations, three utility modules in lib/oracle/.
</objective>

<execution_context>
@C:/Users/ntone/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ntone/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ai-oracle/05-RESEARCH.md
@.planning/phases/05-ai-oracle/05-CONTEXT.md
@packages/db/src/schema/chart-calculations.ts
@packages/db/src/schema/charts.ts
@packages/db/src/schema/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create users and ai_readings database tables</name>
  <files>
    packages/db/src/schema/users.ts
    packages/db/src/schema/ai-readings.ts
    packages/db/src/schema/index.ts
  </files>
  <action>
    Create `packages/db/src/schema/users.ts`:
    - pgTable('users') with columns: id (uuid, defaultRandom, PK), clerk_id (text, notNull, unique), subscription_tier (text, notNull, default 'free'), created_at (timestamp with tz, defaultNow), updated_at (timestamp with tz, defaultNow)
    - NO RLS policies on users table — accessed via service role client only (consistent with chart_calculations pattern). Full RLS + Stripe integration comes in Phase 7.
    - Export User and NewUser types

    Create `packages/db/src/schema/ai-readings.ts`:
    - pgTable('ai_readings') with columns: id (uuid, defaultRandom, PK), chart_id (uuid, notNull, FK to charts.id with onDelete cascade), user_id (text, notNull), topic (text, notNull — 'general' | 'love' | 'career' | 'health'), content (text, notNull — full reading with sentinels stripped), teaser_content (text, nullable — short preview for locked topics), generated_at (timestamp with tz, defaultNow, notNull), expires_at (timestamp with tz, notNull — generated_at + 7 days), last_regenerated_at (timestamp with tz, nullable), model_version (text, notNull — e.g. 'gemini-2.5-flash')
    - Add unique index on (chart_id, topic) — one live reading per chart-topic pair
    - NO RLS — accessed via service role client (consistent with chart_calculations pattern, avoids needing user JWT for server-side onFinish writes)
    - Export AiReading and NewAiReading types

    Update `packages/db/src/schema/index.ts`:
    - Add exports for users (users, User, NewUser)
    - Add exports for aiReadings (aiReadings, AiReading, NewAiReading)

    Generate and apply migration:
    - Run `npx drizzle-kit generate` to create migration SQL
    - Apply migration to Supabase using the same pg client pattern from Phase 4 Plan 2: create a temporary script that reads DATABASE_URL from .env.local and executes the migration SQL via pg client
    - Verify tables exist by querying information_schema
    - Delete temporary migration script after successful apply
  </action>
  <verify>
    1. `npx tsc --noEmit -p packages/db/tsconfig.json` compiles without errors
    2. Query Supabase to confirm both tables exist: `SELECT table_name FROM information_schema.tables WHERE table_name IN ('users', 'ai_readings')`
    3. Verify unique constraint on ai_readings: `SELECT indexname FROM pg_indexes WHERE tablename = 'ai_readings'`
  </verify>
  <done>
    users table exists with clerk_id (unique) and subscription_tier (default 'free'). ai_readings table exists with chart_id FK, topic, content, teaser_content, expires_at, last_regenerated_at, model_version, and unique constraint on (chart_id, topic). Both exported from schema index.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Oracle prompt utilities</name>
  <files>
    apps/web/lib/oracle/prompts.ts
    apps/web/lib/oracle/chart-to-prompt.ts
    apps/web/lib/oracle/planet-parser.ts
  </files>
  <action>
    Create `apps/web/lib/oracle/prompts.ts`:
    - `buildSystemPrompt(topic: 'general' | 'love' | 'career' | 'health'): string`
    - System prompt establishes Celestia mystical guide persona per CONTEXT.md decisions:
      - Elevated, poetic, spiritual overtones — "звездите са изтъкали", "небесните влияния", "вашият космически път"
      - Moderate mysticism: cosmic energy, celestial influences, spiritual path — NO crystal/chakra talk
      - Second person address: "Вашият", "Вие"
      - Instruct AI to write 7-9 paragraphs, each focusing on one placement or interaction
      - Instruct AI to cite exact degrees: "Вашият Слънце на 15 градуса Лъв"
      - Sentinel instruction: wrap planet mentions with [planet:KEY]Bulgarian text[/planet] using English keys (sun, moon, mercury, venus, mars, jupiter, saturn, uranus, neptune, pluto)
      - Include ONE example of correct sentinel usage in the system prompt
      - All instructions in English (system prompt), but tell AI to generate output entirely in Bulgarian
    - Topic suffixes per research (appended to base system prompt):
      - general: personality, life path, core character from Sun, Moon, Ascendant, chart shape
      - love: Venus, 7th house, Moon, relationship aspect patterns — warm tone
      - career: 10th house, MC, Saturn, Sun in vocation context — assertive tone
      - health: 6th house, Mars, vitality and wellness — nurturing tone

    Create `apps/web/lib/oracle/chart-to-prompt.ts`:
    - `chartToPromptText(chartData: ChartData): string`
    - Import ChartData from `@celestia/astrology/client`
    - Serialize planet positions: "{planet}: {signDegree}° {sign}, дом {house}{retrograde ? ' (ретроградна)' : ''}"
    - Include Ascendant and MC with degree and sign
    - Include aspects summary (planet1 aspect planet2, orb)
    - Format as "НАТАЛНА КАРТА:\n{planets}\nАсцендент: ...\nMC: ...\nАспекти: ..."

    Create `apps/web/lib/oracle/planet-parser.ts`:
    - `extractPlanetMentions(text: string): string[]` — returns array of planet keys from [planet:KEY]...[/planet] markers
    - `stripSentinels(text: string): string` — removes sentinel markers, keeps inner text
    - Use regex: /\[planet:(\w+)\](.*?)\[\/planet\]/gs
    - IMPORTANT: Reset regex lastIndex between calls (or create new RegExp each time) to avoid stateful regex bugs
  </action>
  <verify>
    1. `npx tsc --noEmit -p apps/web/tsconfig.json` compiles without errors
    2. Manually verify prompts.ts exports buildSystemPrompt and returns string containing sentinel instruction
    3. Verify planet-parser.ts: extractPlanetMentions('[planet:mars]Марс[/planet]') should return ['mars'], stripSentinels should return 'Марс'
  </verify>
  <done>
    buildSystemPrompt('general') returns a Bulgarian mystical guide system prompt with sentinel markers instruction. chartToPromptText serializes ChartData into textual format. extractPlanetMentions and stripSentinels correctly parse and strip [planet:KEY]...[/planet] markers.
  </done>
</task>

</tasks>

<verification>
1. Both database tables created and accessible
2. All three lib/oracle/ modules compile without TypeScript errors
3. Schema index exports both new tables
4. System prompt contains sentinel instruction and Bulgarian persona
</verification>

<success_criteria>
- users and ai_readings tables exist in Supabase with correct columns and constraints
- lib/oracle/prompts.ts, chart-to-prompt.ts, planet-parser.ts exist and compile
- All exports available from schema/index.ts
</success_criteria>

<output>
After completion, create `.planning/phases/05-ai-oracle/05-01-SUMMARY.md`
</output>
