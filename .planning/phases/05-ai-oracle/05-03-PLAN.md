---
phase: 05-ai-oracle
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - apps/web/components/oracle/OraclePanel.tsx
  - apps/web/components/oracle/TopicCards.tsx
  - apps/web/components/oracle/TopicCard.tsx
  - apps/web/components/oracle/ReadingStream.tsx
  - apps/web/components/oracle/LockedTopicTeaser.tsx
  - apps/web/components/chart/ChartView.tsx
  - apps/web/app/(protected)/chart/page.tsx
autonomous: false

must_haves:
  truths:
    - "User sees topic cards (general, love, career, health) below/beside the chart"
    - "User taps general topic card and sees reading stream in token-by-token"
    - "Free user sees lock icon on premium topic cards"
    - "Free user tapping locked topic sees blurred teaser with upgrade CTA"
    - "When reading mentions a planet, that planet highlights on the natal wheel"
    - "Multiple saved readings are accessible via their topic cards"
    - "Reading panel appears beside chart on desktop, below on mobile"
    - "User can regenerate a reading (rate-limited once per day)"
  artifacts:
    - path: "apps/web/components/oracle/OraclePanel.tsx"
      provides: "Main Oracle side panel container"
      contains: "OraclePanel"
    - path: "apps/web/components/oracle/TopicCards.tsx"
      provides: "Topic card grid with lock states"
      contains: "TopicCards"
    - path: "apps/web/components/oracle/ReadingStream.tsx"
      provides: "Streaming reading display with cross-highlight parsing"
      contains: "ReadingStream"
    - path: "apps/web/components/oracle/LockedTopicTeaser.tsx"
      provides: "Blurred teaser with upgrade CTA overlay"
      contains: "LockedTopicTeaser"
    - path: "apps/web/components/chart/ChartView.tsx"
      provides: "Updated chart view with Oracle panel integration"
      contains: "OraclePanel"
  key_links:
    - from: "apps/web/components/oracle/ReadingStream.tsx"
      to: "apps/web/lib/oracle/planet-parser.ts"
      via: "extractPlanetMentions + stripSentinels imports"
      pattern: "import.*from.*lib/oracle/planet-parser"
    - from: "apps/web/components/oracle/ReadingStream.tsx"
      to: "apps/web/components/chart/NatalWheel.tsx"
      via: "onPlanetHighlight callback -> selectedPlanet state in ChartView"
      pattern: "onPlanetHighlight"
    - from: "apps/web/components/oracle/OraclePanel.tsx"
      to: "apps/web/hooks/useOracleReading.ts"
      via: "useOracleReading hook"
      pattern: "useOracleReading"
    - from: "apps/web/components/oracle/TopicCards.tsx"
      to: "apps/web/components/oracle/LockedTopicTeaser.tsx"
      via: "Renders teaser for locked topics"
      pattern: "LockedTopicTeaser"
---

<objective>
Build the Oracle UI: topic cards, streaming reading display, cross-highlighting, locked topic teaser, and integrate everything with the existing chart page.

Purpose: Deliver the user-facing AI Oracle experience — topic selection, streaming reading with mystical voice, cross-highlighting between reading and chart wheel, and premium-gated content with upgrade motivation.

Output: Five Oracle components, updated ChartView with Oracle panel, updated chart page layout.
</objective>

<execution_context>
@C:/Users/ntone/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ntone/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ai-oracle/05-RESEARCH.md
@.planning/phases/05-ai-oracle/05-CONTEXT.md
@.planning/phases/05-ai-oracle/05-01-SUMMARY.md
@.planning/phases/05-ai-oracle/05-02-SUMMARY.md
@apps/web/components/chart/ChartView.tsx
@apps/web/components/chart/NatalWheel.tsx
@apps/web/app/(protected)/chart/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Oracle UI components</name>
  <files>
    apps/web/components/oracle/OraclePanel.tsx
    apps/web/components/oracle/TopicCards.tsx
    apps/web/components/oracle/TopicCard.tsx
    apps/web/components/oracle/ReadingStream.tsx
    apps/web/components/oracle/LockedTopicTeaser.tsx
  </files>
  <action>
    Create `apps/web/components/oracle/TopicCard.tsx`:
    - 'use client'
    - Props: topic ('general' | 'love' | 'career' | 'health'), label (string), icon (JSX), isLocked (boolean), isActive (boolean), hasSavedReading (boolean), onClick callback
    - Visual card with glassmorphism styling (bg-slate-800/60 backdrop-blur border-slate-700/50)
    - States: default (clickable), active (purple glow border), locked (lock icon overlay, muted colors), has-saved (subtle checkmark indicator)
    - Lock icon (SVG padlock) for premium topics when isLocked=true
    - Topic icons: general = star/sparkle, love = heart, career = briefcase/chart-up, health = leaf/pulse
    - All labels in Bulgarian: "Личност", "Любов", "Кариера", "Здраве"
    - Accessible: role="button", tabindex, keyboard Enter/Space

    Create `apps/web/components/oracle/TopicCards.tsx`:
    - 'use client'
    - Props: subscriptionTier ('free' | 'premium'), activeTopic, savedReadings record, onTopicSelect callback, onLockedTopicTap callback
    - Renders 4 TopicCard components in a 2x2 grid (sm:grid-cols-4 on desktop)
    - Premium topics (love, career, health) show isLocked=true when tier is 'free'
    - Locked topic tap fires onLockedTopicTap instead of onTopicSelect

    Create `apps/web/components/oracle/ReadingStream.tsx`:
    - 'use client'
    - Props: completion (string — streaming text from useCompletion), isLoading (boolean), onPlanetHighlight (planet: string) => void, onComplete callback
    - Uses stripSentinels() from lib/oracle/planet-parser to clean display text
    - Uses extractPlanetMentions() to detect planet references in accumulated text
    - useEffect on completion: when new planet mention detected, call onPlanetHighlight with the most recent planet key
    - Loading state: show "Celestia консултира звездите..." with a pulsing star animation while isLoading=true and completion is empty (pre-first-token)
    - Display: render stripped text with prose-like styling — text-slate-200, leading-relaxed, space-y-4 for paragraphs
    - Split text by double newline into paragraphs for proper formatting
    - Smooth auto-scroll to bottom as text streams in (ref on container, scrollIntoView on update)

    Create `apps/web/components/oracle/LockedTopicTeaser.tsx`:
    - 'use client'
    - Props: topic, teaserContent (string | null), isLoadingTeaser (boolean), onRequestTeaser callback
    - On mount or when teaserContent is null and topic changes: call onRequestTeaser to fetch teaser from /api/oracle/teaser
    - Display: blurred text (filter blur-sm select-none pointer-events-none text-slate-300 leading-relaxed) with absolute-positioned upgrade CTA overlay
    - Upgrade button: "Отключете с Premium" — glassmorphism purple button with gradient bg-gradient-to-r from-purple-600 to-violet-600
    - Loading state while teaser generates: skeleton pulse lines
    - If teaser fails to load, show static placeholder: "Открийте какво звездите разкриват за вашата {topic}..."

    Create `apps/web/components/oracle/OraclePanel.tsx`:
    - 'use client'
    - Props: chartId (string), subscriptionTier ('free' | 'premium'), onPlanetHighlight (planet: string) => void
    - Uses useOracleReading(chartId) hook for all state management
    - Layout: vertical stack — TopicCards at top, reading content below
    - When no topic selected and no saved readings: show instructional text "Изберете тема, за да получите вашето персонализирано четене"
    - When active topic generating: show ReadingStream with streaming completion
    - When saved reading viewed (not generating): show saved content with rendered paragraphs
    - When locked topic tapped: show LockedTopicTeaser for that topic
    - Regenerate button: appears below completed reading — "Ново четене" with refresh icon. Disabled if within rate limit. Tooltip: "Можете да регенерирате веднъж на ден"
    - Reading header: show topic label + generation date for saved readings
    - Glassmorphism container: rounded-xl bg-slate-900/60 backdrop-blur-xl border border-slate-700/50 p-6
    - Max-height with overflow-y-auto for long readings (max-h-[70vh] on desktop)
  </action>
  <verify>
    1. `npx tsc --noEmit -p apps/web/tsconfig.json` compiles without errors
    2. All five component files exist in apps/web/components/oracle/
    3. ReadingStream imports from lib/oracle/planet-parser
    4. OraclePanel imports useOracleReading hook
  </verify>
  <done>
    Five Oracle components created: TopicCard (single card), TopicCards (grid), ReadingStream (streaming display with cross-highlight), LockedTopicTeaser (blurred preview + CTA), OraclePanel (container orchestrating all states). All styled with glassmorphism, Bulgarian labels, accessible.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate OraclePanel with ChartView and chart page</name>
  <files>
    apps/web/components/chart/ChartView.tsx
    apps/web/app/(protected)/chart/page.tsx
  </files>
  <action>
    Update `apps/web/components/chart/ChartView.tsx`:
    - Add new prop: subscriptionTier ('free' | 'premium'), default to 'free'
    - Import OraclePanel from '../oracle/OraclePanel'
    - Add onPlanetHighlight handler that bridges Oracle cross-highlighting to NatalWheel:
      - Map planet key (e.g. 'mars') to find matching PlanetPosition in chart.planets
      - Call existing setSelectedPlanet and setSelectedPlanetData to highlight the planet on the wheel
    - Layout change for desktop (lg+):
      - Current: wheel (flex-1) + BigThreeCards (w-64)
      - New: wheel (flex-1) + right column (w-96) containing BigThreeCards THEN OraclePanel below
      - The right column becomes a vertical stack: BigThreeCards at top, OraclePanel below with flex-1 to fill remaining space
    - Layout change for mobile:
      - Current: BigThreeCards above wheel
      - New: BigThreeCards above wheel, OraclePanel below wheel (full width)
    - Pass chart.id (need to add chartId to ChartView's data or derive from props) and subscriptionTier to OraclePanel

    Update `apps/web/app/(protected)/chart/page.tsx`:
    - Fetch user's subscription tier from users table via service role client (by clerk_id = userId)
    - If no user row found, default to 'free' (user will be auto-created on first Oracle generation)
    - Pass subscriptionTier prop to ChartView
    - Widen max-w container from max-w-5xl to max-w-7xl to accommodate the Oracle panel on desktop
  </action>
  <verify>
    1. `npx tsc --noEmit -p apps/web/tsconfig.json` compiles without errors
    2. `npm run build` succeeds (or at least the chart page compiles)
    3. ChartView renders OraclePanel component
    4. chart/page.tsx queries users table for subscription tier
  </verify>
  <done>
    ChartView integrates OraclePanel in the right column (desktop) or below wheel (mobile). Cross-highlighting bridges Oracle planet mentions to NatalWheel selection state. Chart page fetches subscription tier and passes to ChartView. Layout widened to accommodate Oracle panel.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify AI Oracle end-to-end</name>
  <files>apps/web/components/oracle/OraclePanel.tsx</files>
  <action>
    Human verification of complete AI Oracle feature. What was built: topic cards, streaming AI reading generation via Gemini, cross-highlighting between reading text and natal wheel, locked topic teasers with upgrade CTA, saved readings with 7-day expiry, regeneration rate limiting.

    Prerequisites: Ensure GOOGLE_GENERATIVE_AI_API_KEY is set in apps/web/.env.local

    Steps to verify:
    1. Start dev server: `npm run dev`
    2. Navigate to /chart page (must have birth data and a calculated chart)
    3. Verify topic cards appear: "Личност" (general), "Любов" (love), "Кариера" (career), "Здраве" (health)
    4. As a free user: verify love/career/health cards show lock icon
    5. Tap "Личност" (general) card:
       - Should see loading state "Celestia консултира звездите..."
       - Reading should stream in token by token (not all at once)
       - Reading should be in Bulgarian with mystical tone
       - Reading should cite specific degree positions
       - When reading mentions a planet, that planet should highlight on the wheel
    6. After reading completes, verify "Личност" card shows checkmark/indicator
    7. Tap a locked topic (e.g., "Любов"):
       - Should see blurred teaser text with "Отключете с Premium" button overlay
    8. Refresh page — verify the general reading loads from cache (instant, not re-streaming)
    9. Desktop: verify Oracle panel is beside the chart in the right column
    10. Mobile (resize browser): verify Oracle panel is below the chart
  </action>
  <verify>User confirms all 10 verification steps pass. Type "approved" or describe issues.</verify>
  <done>AI Oracle feature verified by user: streaming, cross-highlighting, tier gating, caching, and responsive layout all working.</done>
</task>

</tasks>

<verification>
1. All Oracle components render without errors
2. Streaming works token-by-token (not buffered)
3. Cross-highlighting fires when reading mentions planets
4. Locked topics show blurred teaser with CTA
5. Saved readings persist across page refresh
6. Layout is responsive (side panel on desktop, below on mobile)
7. All text in Bulgarian
</verification>

<success_criteria>
- Free user sees general personality reading generated from their chart (AI-01, AI-02)
- Free user tapping love/career/health sees upgrade prompt (AI-02)
- All readings cite specific degree positions (AI-06)
- Readings feel personalized, not generic (AI-06)
- Cross-highlighting works between reading and chart wheel
- Multiple readings saved and accessible via topic cards
- Responsive layout works on desktop and mobile
</success_criteria>

<output>
After completion, create `.planning/phases/05-ai-oracle/05-03-SUMMARY.md`
</output>
