---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - apps/web/package.json
  - apps/web/next.config.js
  - apps/web/tailwind.config.ts
  - apps/web/postcss.config.js
  - apps/web/app/globals.css
  - apps/web/app/layout.tsx
  - packages/ui/package.json
  - packages/ui/primitives/GlassCard.tsx
  - packages/ui/primitives/Text.tsx
  - packages/ui/index.ts
autonomous: true

must_haves:
  truths:
    - "App displays dark space-themed background color"
    - "Text renders in white/light colors against dark background"
    - "GlassCard component shows glassmorphism effect (blur, transparency, border)"
  artifacts:
    - path: "apps/web/tailwind.config.ts"
      provides: "Tailwind configuration with cosmic theme colors"
      contains: "background"
    - path: "apps/web/app/globals.css"
      provides: "CSS variables for theme tokens"
      contains: "--color-background"
    - path: "packages/ui/primitives/GlassCard.tsx"
      provides: "Glassmorphism card component"
      exports: ["GlassCard"]
  key_links:
    - from: "apps/web/tailwind.config.ts"
      to: "apps/web/app/globals.css"
      via: "CSS variable references"
      pattern: "var\\(--color"
    - from: "apps/web/app/layout.tsx"
      to: "apps/web/app/globals.css"
      via: "CSS import"
      pattern: "import.*globals.css"
---

<objective>
Configure NativeWind v4 with Tailwind CSS and cosmic glassmorphism design tokens.

Purpose: Establish the visual foundation for the "cosmic glassmorphism" dark theme (UI-01) that all UI components will use.
Output: Working theme with CSS variables, glassmorphism primitives, and dark space aesthetic.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install and configure NativeWind with Tailwind CSS</name>
  <files>
    apps/web/package.json
    apps/web/next.config.js
    apps/web/tailwind.config.ts
    apps/web/postcss.config.js
  </files>
  <action>
Install NativeWind v4 and Tailwind CSS 3.4.x in the web app:

1. Add dependencies to `apps/web/package.json`:
```json
{
  "dependencies": {
    "nativewind": "^4.1.23",
    "react-native-css-interop": "^0.1.22"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17",
    "postcss": "^8.5.1",
    "autoprefixer": "^10.4.20"
  }
}
```

Note: Use Tailwind v3.4.x (NOT v4) as NativeWind v4 requires Tailwind v3.

2. Update `apps/web/next.config.js` with transpilePackages:
```javascript
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  transpilePackages: ['nativewind', 'react-native-css-interop'],
  experimental: {
    forceSwcTransforms: true,
  },
}

module.exports = nextConfig
```

3. Create `apps/web/tailwind.config.ts`:
```typescript
import type { Config } from 'tailwindcss'

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
    '../../packages/ui/**/*.{js,ts,jsx,tsx}',
  ],
  presets: [require('nativewind/preset')],
  theme: {
    extend: {
      colors: {
        // Cosmic theme colors using CSS variables
        background: 'rgb(var(--color-background) / <alpha-value>)',
        surface: 'rgb(var(--color-surface) / <alpha-value>)',
        'surface-glass': 'rgb(var(--color-surface-glass) / <alpha-value>)',
        primary: 'rgb(var(--color-primary) / <alpha-value>)',
        secondary: 'rgb(var(--color-secondary) / <alpha-value>)',
        foreground: 'rgb(var(--color-text) / <alpha-value>)',
        muted: 'rgb(var(--color-text-muted) / <alpha-value>)',
      },
      backdropBlur: {
        glass: 'var(--glass-blur)',
      },
    },
  },
  plugins: [],
}

export default config
```

4. Create `apps/web/postcss.config.js`:
```javascript
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
```

Run `pnpm install` after updating package.json.
  </action>
  <verify>
Run `pnpm install` - completes without errors.
Run `pnpm dev` - starts without Tailwind/PostCSS errors.
  </verify>
  <done>
NativeWind v4 and Tailwind CSS 3.4.x configured with cosmic theme color system.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create CSS variables for cosmic glassmorphism theme</name>
  <files>
    apps/web/app/globals.css
    apps/web/app/layout.tsx
  </files>
  <action>
Create the cosmic glassmorphism theme with CSS custom properties:

1. Create `apps/web/app/globals.css`:
```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  :root {
    /* Cosmic Glassmorphism Theme - Dark Space Aesthetic */

    /* Core colors (RGB values for Tailwind alpha support) */
    --color-background: 10 8 25;          /* Deep space navy #0a0819 */
    --color-surface: 20 16 45;            /* Elevated surface #14102d */
    --color-surface-glass: 35 30 65;      /* Glassmorphism layer #231e41 */

    /* Accent colors */
    --color-primary: 139 92 246;          /* Violet #8b5cf6 */
    --color-secondary: 236 72 153;        /* Pink #ec4899 */

    /* Text colors */
    --color-text: 255 255 255;            /* White */
    --color-text-muted: 156 163 175;      /* Gray-400 */

    /* Glassmorphism tokens */
    --glass-blur: 16px;
    --glass-opacity: 0.15;
    --glass-border: rgba(255, 255, 255, 0.1);

    /* Gradients */
    --gradient-cosmic: linear-gradient(135deg, rgb(139, 92, 246) 0%, rgb(236, 72, 153) 100%);
  }
}

/* Base body styles */
body {
  @apply bg-background text-foreground antialiased;
  min-height: 100vh;
}

/* Glassmorphism utility class */
.glass {
  background: rgb(var(--color-surface-glass) / var(--glass-opacity));
  backdrop-filter: blur(var(--glass-blur));
  -webkit-backdrop-filter: blur(var(--glass-blur));
  border: 1px solid var(--glass-border);
}
```

2. Update `apps/web/app/layout.tsx` to import globals.css:
```tsx
import type { Metadata } from 'next'
import './globals.css'

export const metadata: Metadata = {
  title: 'Celestia AI - Вашият астрологичен спътник',
  description: 'Персонализирани хороскопи и астрологични прогнози',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="bg" className="dark">
      <body className="min-h-screen bg-background text-foreground antialiased">
        {children}
      </body>
    </html>
  )
}
```

Note: `lang="bg"` for Bulgarian language (UI-02), `className="dark"` ensures dark theme.
  </action>
  <verify>
Run `pnpm dev` and visit http://localhost:3000.
Page should show deep space dark background (#0a0819).
Text should be white against dark background.
  </verify>
  <done>
CSS variables defined for cosmic glassmorphism theme, dark space background visible.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create glassmorphism UI primitives</name>
  <files>
    packages/ui/package.json
    packages/ui/primitives/GlassCard.tsx
    packages/ui/primitives/Text.tsx
    packages/ui/index.ts
  </files>
  <action>
Create shared UI primitives for the cosmic theme:

1. Update `packages/ui/package.json` to add React:
```json
{
  "name": "@celestia/ui",
  "version": "0.1.0",
  "private": true,
  "main": "./index.ts",
  "types": "./index.ts",
  "exports": {
    ".": "./index.ts",
    "./primitives": "./primitives/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "react": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.8",
    "typescript": "^5.7.3"
  },
  "peerDependencies": {
    "react": "^19.0.0"
  }
}
```

2. Create `packages/ui/primitives/GlassCard.tsx`:
```tsx
import React from 'react'

interface GlassCardProps {
  children: React.ReactNode
  className?: string
}

/**
 * Glassmorphism card component for cosmic theme.
 * Uses CSS variables from globals.css for consistent theming.
 */
export function GlassCard({ children, className = '' }: GlassCardProps) {
  return (
    <div
      className={`
        bg-surface-glass/15
        backdrop-blur-glass
        border border-white/10
        rounded-2xl
        p-6
        shadow-xl
        ${className}
      `.trim()}
    >
      {children}
    </div>
  )
}
```

3. Create `packages/ui/primitives/Text.tsx`:
```tsx
import React from 'react'

interface TextProps {
  children: React.ReactNode
  variant?: 'h1' | 'h2' | 'h3' | 'body' | 'muted'
  className?: string
  as?: keyof JSX.IntrinsicElements
}

const variantStyles: Record<string, string> = {
  h1: 'text-4xl font-bold text-foreground',
  h2: 'text-2xl font-semibold text-foreground',
  h3: 'text-xl font-medium text-foreground',
  body: 'text-base text-foreground',
  muted: 'text-sm text-muted',
}

const variantTags: Record<string, keyof JSX.IntrinsicElements> = {
  h1: 'h1',
  h2: 'h2',
  h3: 'h3',
  body: 'p',
  muted: 'p',
}

/**
 * Text component with semantic variants for cosmic theme.
 */
export function Text({
  children,
  variant = 'body',
  className = '',
  as,
}: TextProps) {
  const Tag = as || variantTags[variant] || 'p'
  const styles = variantStyles[variant] || variantStyles.body

  return (
    <Tag className={`${styles} ${className}`.trim()}>
      {children}
    </Tag>
  )
}
```

4. Create `packages/ui/primitives/index.ts`:
```typescript
export { GlassCard } from './GlassCard'
export { Text } from './Text'
```

5. Update `packages/ui/index.ts`:
```typescript
export * from './primitives'
```

Create directory: `packages/ui/primitives/`

Run `pnpm install` to link the updated package.
  </action>
  <verify>
Run `pnpm typecheck` - no errors in @celestia/ui.
Import GlassCard in a test file - should export correctly.
  </verify>
  <done>
GlassCard and Text primitives created with cosmic glassmorphism styling.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Visual check:**
   - http://localhost:3000 shows deep space dark background
   - Text is white/light colored against dark background
   - No default white/light background visible anywhere

2. **Theme tokens check:**
   - `globals.css` contains all CSS variables
   - `tailwind.config.ts` references CSS variables
   - Colors use `rgb(var(--color-*))` pattern for alpha support

3. **Component check:**
   - `@celestia/ui` exports GlassCard and Text
   - Components use Tailwind classes with theme colors
   - TypeScript types are correct
</verification>

<success_criteria>
- UI-01 (cosmic glassmorphism): Dark space background (#0a0819) visible
- UI-02 (Bulgarian): `lang="bg"` set on html element
- NativeWind v4 + Tailwind 3.4.x configured without errors
- CSS variables provide consistent theme tokens
- GlassCard component provides glassmorphism effect
- Text component provides semantic typography variants
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
