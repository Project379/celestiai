---
phase: 08-launch-prep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/components/StarCanvas.tsx
  - apps/web/app/page.tsx
  - apps/web/components/landing/LandingNav.tsx
  - apps/web/components/landing/FeaturesSection.tsx
  - apps/web/components/landing/PricingSection.tsx
autonomous: true

must_haves:
  truths:
    - "Visitor sees animated starfield background in the hero section"
    - "Visitor sees Bulgarian motto/tagline conveying Celestia AI value proposition"
    - "Visitor sees secondary CTA in hero and primary CTA after feature showcase"
    - "Visitor sees 4 feature cards with Lucide icons and Premium badges on Oracle/horoscope"
    - "Visitor can navigate to /auth from landing page CTAs and nav links"
    - "Visitor sees footer with privacy policy link to /privacy"
    - "Pricing section links to /pricing page (not duplicating checkout logic)"
  artifacts:
    - path: "apps/web/components/StarCanvas.tsx"
      provides: "Reusable star canvas animation extracted from AuthBackground"
      contains: "StarCanvas"
    - path: "apps/web/app/page.tsx"
      provides: "Landing page with hero starfield, CTAs, footer with privacy link"
      contains: "StarCanvas"
    - path: "apps/web/components/landing/FeaturesSection.tsx"
      provides: "Feature cards with Lucide icons and Premium badges"
      contains: "lucide-react"
    - path: "apps/web/components/landing/LandingNav.tsx"
      provides: "Nav with auth links pointing to /auth"
      contains: "/auth"
  key_links:
    - from: "apps/web/app/page.tsx"
      to: "apps/web/components/StarCanvas.tsx"
      via: "import StarCanvas"
      pattern: "import.*StarCanvas"
    - from: "apps/web/components/landing/FeaturesSection.tsx"
      to: "lucide-react"
      via: "icon imports"
      pattern: "from 'lucide-react'"
    - from: "apps/web/components/landing/LandingNav.tsx"
      to: "/auth"
      via: "href"
      pattern: 'href="/auth"'
---

<objective>
Upgrade the landing page into a conversion-focused experience with animated starfield hero, Lucide icon feature showcase with Premium badges, proper auth links, and footer privacy link.

Purpose: LAND-01 (stars background + motto), LAND-02 (pricing comparison), LAND-03 (feature showcase), LAND-04 (navigate to login/registration), SEC-03 (privacy link from landing).
Output: Enhanced landing page with hero, upgraded feature cards, corrected auth links, footer privacy link.
</objective>

<execution_context>
@C:/Users/ntone/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ntone/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-launch-prep/08-CONTEXT.md
@.planning/phases/08-launch-prep/08-RESEARCH.md
@apps/web/components/auth/AuthBackground.tsx
@apps/web/app/page.tsx
@apps/web/components/landing/LandingNav.tsx
@apps/web/components/landing/FeaturesSection.tsx
@apps/web/components/landing/PricingSection.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract StarCanvas component and upgrade landing page hero + footer</name>
  <files>
    apps/web/components/StarCanvas.tsx
    apps/web/app/page.tsx
    apps/web/components/landing/LandingNav.tsx
    apps/web/components/landing/PricingSection.tsx
  </files>
  <action>
**1. Install lucide-react:**
```bash
cd apps/web && pnpm add lucide-react
```

**2. Create `apps/web/components/StarCanvas.tsx`:**

Extract ONLY the star canvas animation logic from `AuthBackground.tsx` into a standalone reusable component. This component renders just the `<canvas>` element with the twinkling star animation (the `useEffect` with requestAnimationFrame, star generation, twinkle drawing). NO gradient background, NO nebula glow divs — just the canvas. The component should:
- Accept optional `className` prop for positioning
- Accept optional `starCount` prop (default 150)
- Be a `'use client'` component
- Use the exact same Star interface, animation math, and resize handler from AuthBackground
- Canvas should have `aria-hidden="true"`
- Position: `absolute inset-0` by default (parent controls containment)

**3. Modify `apps/web/app/page.tsx`:**

Restructure the landing page with the "explore then convert" approach:

a) **Hero section** — wrap in a `relative overflow-hidden` container with a dark gradient background. Render `<StarCanvas />` inside this container (positioned absolute, behind content). Hero content:
   - Celestia AI heading (keep existing gradient text style)
   - Bulgarian placeholder motto: `"Звездите разказват вашата история. Ние я превеждаме."` (The stars tell your story. We translate it.)
   - Subtitle: `"Персонализирана астрология, задвижвана от изкуствен интелект"` (Personalized astrology, powered by AI)
   - **Secondary CTA button** linking to `/auth`: `"Открийте звездите"` (Discover the stars) — purple gradient button style matching existing
   - Hero should have generous padding (py-32 md:py-40)
   - The hero background gradient: from slate-900 via indigo-950/50 to slate-900 (cosmic feel, NOT the full AuthBackground gradient)

b) **After FeaturesSection**, add a **primary CTA block** — centered section with compelling text and a larger CTA button:
   - Text: `"Готови ли сте да разберете какво ви казват звездите?"` (Ready to understand what the stars tell you?)
   - CTA button linking to `/auth`: `"Започнете безплатно"` (Start for free) — same gradient button style but larger (px-10 py-4 text-lg)

c) **Footer** — enhance existing footer to include a privacy policy link:
   - Keep copyright text
   - Add `<Link href="/privacy">Политика за поверителност</Link>` (Privacy policy) styled as text-slate-500 hover:text-slate-400 link
   - Layout: flex justify-between on md+, stacked on mobile

d) **Fix hero CTA link** — change the existing `href="/sign-up"` to `href="/auth"` per Phase 2 decision (combined auth route)

**4. Fix `apps/web/components/landing/LandingNav.tsx`:**
- Change `href="/sign-in"` to `href="/auth"` for "Вход" link
- Change `href="/sign-up"` to `href="/auth"` for "Регистрация" link
- This fixes Pitfall 2 from research: all auth CTAs must point to /auth

**5. Fix `apps/web/components/landing/PricingSection.tsx`:**
- Change both `href="/sign-up"` links to `href="/pricing"` for the "Започнете безплатно" and "Опитайте Премиум" buttons — per context decision, landing pricing should link to the full /pricing page rather than duplicating checkout logic
  </action>
  <verify>
Run `cd apps/web && npx next build 2>&1 | head -30` — build should compile without errors. Verify StarCanvas.tsx exists and exports StarCanvas component. Verify page.tsx imports StarCanvas. Verify no remaining `/sign-up` or `/sign-in` hrefs in landing components (grep for them).
  </verify>
  <done>
Landing page has animated starfield hero with Bulgarian motto, secondary CTA in hero, primary CTA after features, footer with privacy link, all auth links point to /auth, pricing links point to /pricing.
  </done>
</task>

<task type="auto">
  <name>Task 2: Upgrade FeaturesSection with Lucide icons and Premium badges</name>
  <files>
    apps/web/components/landing/FeaturesSection.tsx
  </files>
  <action>
Rewrite `FeaturesSection.tsx` to use Lucide-react icons and add Premium badges per CONTEXT.md decisions:

**Feature cards (4 total, up from 3):**

1. **Натална карта** (Natal Chart) — free tier
   - Icon: `Star` from lucide-react, `className="h-8 w-8 text-purple-400"`
   - Description: `"Разкрийте вашата уникална астрологична карта на раждане с точни планетарни позиции и интерактивна визуализация."`
   - No Premium badge

2. **AI Оракул** (AI Oracle) — Premium badge
   - Icon: `Sparkles` from lucide-react, `className="h-8 w-8 text-violet-400"`
   - Description: `"Интелигентни персонализирани тълкувания за любов, кариера и здраве, създадени от изкуствен интелект."`
   - **Premium badge** — absolute positioned top-right of card

3. **Дневен хороскоп** (Daily Horoscope) — Premium badge on "full access"
   - Icon: `Calendar` from lucide-react, `className="h-8 w-8 text-indigo-400"`
   - Description: `"Персонализирани дневни прогнози базирани на транзитите към вашата натална карта. Пълен достъп с Премиум."`
   - **Premium badge** — absolute positioned top-right of card

4. **Известия** (Push Notifications) — free tier
   - Icon: `Bell` from lucide-react, `className="h-8 w-8 text-amber-400"`
   - Description: `"Получавайте сутрешно известие, за да не пропуснете вашия дневен хороскоп."`
   - No Premium badge

**Premium badge styling** — must visually match the existing premium badge from the dashboard (from Phase 7). Use:
```tsx
<span className="absolute -top-2 -right-2 rounded-full bg-gradient-to-r from-purple-600 to-violet-600 px-2.5 py-0.5 text-xs font-medium text-white">
  Premium
</span>
```

**Grid layout:** Keep the existing `grid-cols-1 md:grid-cols-2 lg:grid-cols-3` but since there are now 4 cards, use `lg:grid-cols-4` for the 4-card layout (or `md:grid-cols-2` with 2x2 for cleaner look on medium screens — use `lg:grid-cols-2` for a 2x2 grid that's visually balanced, since 4 items in a 3-col grid leaves an orphan).

Decision: Use `md:grid-cols-2` (2x2 grid) since 4 cards in 2x2 is cleaner than 4 in a row or 3+1. Max width `max-w-4xl` to keep cards readable.

Each card needs `relative` class on the GlassCard wrapper for the absolute-positioned Premium badge.

Keep imports: `GlassCard, Text` from `@celestia/ui`.
  </action>
  <verify>
Run `cd apps/web && npx next build 2>&1 | head -30` — build compiles. Grep FeaturesSection.tsx for `lucide-react` import, `Premium` badge text, and verify 4 feature objects in the array.
  </verify>
  <done>
FeaturesSection shows 4 feature cards with Lucide icons (Star, Sparkles, Calendar, Bell), Premium badges on AI Oracle and Daily Horoscope cards, clean 2x2 grid layout.
  </done>
</task>

</tasks>

<verification>
1. `cd apps/web && npx next build` compiles without errors
2. `grep -r '/sign-up\|/sign-in' apps/web/components/landing/ apps/web/app/page.tsx` returns no results (all auth links fixed)
3. `grep 'StarCanvas' apps/web/app/page.tsx` confirms starfield is used in hero
4. `grep 'lucide-react' apps/web/components/landing/FeaturesSection.tsx` confirms Lucide icons
5. `grep 'Premium' apps/web/components/landing/FeaturesSection.tsx` confirms Premium badges
6. `grep '/privacy' apps/web/app/page.tsx` confirms footer privacy link
7. `grep '/auth' apps/web/components/landing/LandingNav.tsx` confirms nav auth links
</verification>

<success_criteria>
- Landing page loads with animated starfield in hero section
- Hero shows Bulgarian motto and secondary CTA to /auth
- Feature showcase has 4 cards with Lucide icons and 2 Premium badges
- Primary CTA appears after feature showcase
- Footer includes privacy policy link to /privacy
- All navigation auth links point to /auth
- Pricing links point to /pricing
- Build compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-launch-prep/08-01-SUMMARY.md`
</output>
