---
phase: 04-astrology-engine-charts
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - apps/web/hooks/useD3.ts
  - apps/web/hooks/useChart.ts
  - apps/web/components/chart/NatalWheel.tsx
  - apps/web/components/chart/BigThreeCards.tsx
  - apps/web/components/chart/ChartView.tsx
  - apps/web/app/(dashboard)/chart/page.tsx
  - apps/web/package.json
autonomous: true

must_haves:
  truths:
    - "User with birth data sees natal chart wheel visualization"
    - "Chart displays all 10 planets positioned on the wheel"
    - "User sees Big Three (Sun, Moon, Rising) in prominent cards"
    - "Chart renders correctly on desktop (wheel + cards side by side)"
    - "Chart renders correctly on mobile (cards above wheel)"
  artifacts:
    - path: "apps/web/components/chart/NatalWheel.tsx"
      provides: "D3.js wheel chart visualization"
      min_lines: 100
    - path: "apps/web/components/chart/BigThreeCards.tsx"
      provides: "Sun, Moon, Rising sign cards"
      min_lines: 30
    - path: "apps/web/components/chart/ChartView.tsx"
      provides: "Combined layout with wheel and cards"
      min_lines: 40
    - path: "apps/web/app/(dashboard)/chart/page.tsx"
      provides: "Chart page that fetches and displays chart"
      min_lines: 30
    - path: "apps/web/hooks/useChart.ts"
      provides: "Hook for fetching chart calculation"
      exports: ["useChart"]
  key_links:
    - from: "apps/web/components/chart/NatalWheel.tsx"
      to: "d3"
      via: "import * as d3 from 'd3'"
      pattern: "d3\\.(arc|select|selectAll)"
    - from: "apps/web/hooks/useChart.ts"
      to: "/api/chart/calculate"
      via: "fetch"
      pattern: "fetch.*api/chart/calculate"
    - from: "apps/web/app/(dashboard)/chart/page.tsx"
      to: "apps/web/components/chart/ChartView.tsx"
      via: "import"
      pattern: "import.*ChartView"
---

<objective>
Create the interactive natal chart visualization with D3.js wheel and Big Three cards.

Purpose: Users can see their calculated natal chart as an interactive visualization with all 10 planets, zodiac signs, and houses. Big Three (Sun, Moon, Rising) are prominently displayed for quick scanning.

Output: /chart page with complete natal wheel visualization and Big Three cards.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-astrology-engine-charts/04-02-SUMMARY.md
@.planning/phases/04-astrology-engine-charts/04-RESEARCH.md
@.planning/phases/04-astrology-engine-charts/04-CONTEXT.md
@apps/web/components/dashboard/DashboardContent.tsx
@packages/astrology/src/types.ts
@packages/astrology/src/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create D3 hooks and chart data fetching</name>
  <files>
    apps/web/package.json
    apps/web/hooks/useD3.ts
    apps/web/hooks/useChart.ts
  </files>
  <action>
Set up D3.js and data fetching infrastructure:

1. Add D3 dependencies to apps/web/package.json:
   - "d3": "^7.9.0"
   - "@types/d3": "^7.4.3"
   - Run pnpm install

2. apps/web/hooks/useD3.ts (from RESEARCH.md pattern):
   ```typescript
   'use client'
   import { useRef, useEffect, DependencyList } from 'react'
   import * as d3 from 'd3'

   export function useD3<T extends SVGSVGElement = SVGSVGElement>(
     renderFn: (svg: d3.Selection<T, unknown, null, undefined>) => void,
     deps: DependencyList = []
   ) {
     const ref = useRef<T>(null)

     useEffect(() => {
       if (!ref.current) return
       // Clear previous content
       d3.select(ref.current).selectAll('*').remove()
       // Render new content
       renderFn(d3.select(ref.current))
     }, deps)

     return ref
   }
   ```

3. apps/web/hooks/useChart.ts:
   - Custom hook for fetching chart calculation
   - Parameters: chartId (string | undefined)
   - Returns: { chart: ChartData | null, isLoading: boolean, error: string | null }
   - Uses useSWR or useState + useEffect pattern
   - Calls POST /api/chart/calculate with chartId
   - Handles loading and error states
   - Import ChartData type from @celestia/astrology
  </action>
  <verify>
    - D3 is installed: `pnpm list d3` shows version 7.x
    - useD3 hook TypeScript compiles without errors
    - useChart hook can be imported and called
  </verify>
  <done>D3 integration hooks and chart data fetching ready</done>
</task>

<task type="auto">
  <name>Task 2: Create NatalWheel D3 visualization component</name>
  <files>
    apps/web/components/chart/NatalWheel.tsx
  </files>
  <action>
Create the D3.js natal chart wheel component:

1. apps/web/components/chart/NatalWheel.tsx:
   - 'use client' directive
   - Props: { chart: ChartData, onPlanetSelect: (planet) => void, size?: number }
   - Uses useD3 hook for rendering

   Structure (concentric rings from outside to inside):
   a. Outer ring: 12 zodiac sign segments (30 degrees each)
      - Alternating background colors (subtle, cosmic theme)
      - Sign abbreviations in Bulgarian (Ов, Те, Бл, etc.) or full names
      - Aries starts at 9 o'clock position (standard chart orientation)

   b. House divisions: Lines from center marking house cusps
      - Variable angles based on Placidus calculation
      - House numbers (1-12) at each division

   c. Planets: Positioned on inner circle at their longitude
      - Use circles with planet abbreviations or symbols
      - Colors: Sun=gold, Moon=silver, Mars=red, etc.
      - Click handler calls onPlanetSelect

   d. Aspect lines (optional, can be toggled):
      - Lines connecting planets with aspects
      - Color coded: trine=blue, square=red, etc.
      - Dashed or solid based on aspect type

   Styling (from CONTEXT.md):
   - Dark background
   - Thin lines with subtle glow
   - Glassmorphism aesthetic
   - Use Tailwind colors where possible

   Responsive:
   - size prop defaults to 500
   - viewBox makes it scale
  </action>
  <verify>
    - Component renders SVG wheel with 12 zodiac segments
    - Planets appear at correct positions
    - Clicking a planet triggers onPlanetSelect callback
    - Wheel scales properly with different size prop values
  </verify>
  <done>NatalWheel renders interactive chart wheel with planets and houses</done>
</task>

<task type="auto">
  <name>Task 3: Create BigThreeCards and ChartView layout</name>
  <files>
    apps/web/components/chart/BigThreeCards.tsx
    apps/web/components/chart/ChartView.tsx
    apps/web/app/(dashboard)/chart/page.tsx
  </files>
  <action>
Create Big Three cards and combine into chart page:

1. apps/web/components/chart/BigThreeCards.tsx:
   - 'use client' directive
   - Props: { sun: PlanetPosition, moon: PlanetPosition, ascendant: { sign, degree }, onSelect: (type) => void }

   Three glassmorphism cards:
   - Слънце (Sun): sign in Bulgarian, brief trait
   - Луна (Moon): sign in Bulgarian, brief trait
   - Възходящ (Rising/Ascendant): sign in Bulgarian, brief trait

   Traits are hardcoded placeholders per sign (brief, one word):
   - Овен: "лидер", Телец: "стабилен", etc.

   Card styling:
   - backdrop-blur-sm bg-white/5 (glassmorphism)
   - Border glow effect
   - Icon or emoji for each (optional)
   - Clickable: calls onSelect('sun'|'moon'|'rising')

   If ascendant unavailable (unknown birth time):
   - Show "~" prefix: "~Възходящ"
   - Tooltip or note: "приблизително"

2. apps/web/components/chart/ChartView.tsx:
   - 'use client' directive
   - Props: { chartId: string }
   - Uses useChart hook to fetch data
   - Layout:
     - Desktop (lg+): Big Three cards on right, wheel on left
     - Mobile: Big Three cards above, wheel below
   - Loading state: skeleton
   - Error state: Bulgarian error message

   Manages selected planet state for highlighting (passed to NatalWheel)

3. apps/web/app/(dashboard)/chart/page.tsx:
   - Server component that gets chartId from user's charts
   - Fetch user's primary chart (first chart) on server
   - If no chart exists, show CTA to add birth data
   - Pass chartId to ChartView client component

   Bulgarian text:
   - Page title: "Вашата натална карта"
   - No chart CTA: "Добавете рождени данни"
  </action>
  <verify>
    - /chart page loads without errors
    - Big Three cards display Sun, Moon, Rising with Bulgarian sign names
    - Wheel displays with planets positioned correctly
    - Layout is responsive (cards beside wheel on desktop, above on mobile)
    - Loading skeleton appears while fetching
    - Error message appears if calculation fails
  </verify>
  <done>Chart page displays complete natal chart with wheel and Big Three cards</done>
</task>

</tasks>

<verification>
1. Navigate to /chart shows natal wheel with all 10 planets
2. Zodiac signs labeled in Bulgarian around the wheel
3. Big Three cards show Sun, Moon, Rising signs prominently
4. Layout is responsive (side-by-side on desktop, stacked on mobile)
5. Clicking planet triggers selection (console.log for now)
6. Unknown birth time shows disclaimer on Rising sign
7. No chart state shows "Добавете рождени данни" CTA
</verification>

<success_criteria>
- Chart visualization displays all 10 major planets (CHART-05)
- Interactive chart is visible (CHART-02)
- Big Three (Sun, Moon, Rising) prominently displayed (CHART-04)
- Chart renders correctly on desktop and mobile
- Bulgarian text throughout
- Glassmorphism styling matches app theme
</success_criteria>

<output>
After completion, create `.planning/phases/04-astrology-engine-charts/04-03-SUMMARY.md`
</output>
