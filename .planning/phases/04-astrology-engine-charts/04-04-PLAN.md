---
phase: 04-astrology-engine-charts
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - apps/web/components/chart/PlanetDetail.tsx
  - apps/web/components/chart/NatalWheel.tsx
  - apps/web/components/chart/BigThreeCards.tsx
  - apps/web/components/chart/ChartView.tsx
  - apps/web/lib/interpretations/planets.ts
autonomous: true

must_haves:
  truths:
    - "User can tap/click any planet to see interpretation panel"
    - "Interpretation shows planet position in Bulgarian"
    - "Interpretation shows placeholder text for coming AI features"
    - "Big Three cards expand to show detail when tapped"
    - "Selected planet is visually highlighted on the wheel"
  artifacts:
    - path: "apps/web/components/chart/PlanetDetail.tsx"
      provides: "Planet interpretation popup/panel"
      min_lines: 50
    - path: "apps/web/lib/interpretations/planets.ts"
      provides: "Placeholder interpretations in Bulgarian"
      exports: ["getPlanetInterpretation"]
  key_links:
    - from: "apps/web/components/chart/ChartView.tsx"
      to: "apps/web/components/chart/PlanetDetail.tsx"
      via: "import"
      pattern: "import.*PlanetDetail"
    - from: "apps/web/components/chart/PlanetDetail.tsx"
      to: "apps/web/lib/interpretations/planets.ts"
      via: "import"
      pattern: "import.*getPlanetInterpretation"
---

<objective>
Add planet interpretation popups and interaction to complete the chart experience.

Purpose: Users can tap any planet on the chart or Big Three cards to see detailed information about that placement. Phase 4 shows position data with placeholder text, full AI interpretations come in Phase 5.

Output: Interactive chart where clicking planets reveals interpretation panels.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-astrology-engine-charts/04-03-SUMMARY.md
@.planning/phases/04-astrology-engine-charts/04-CONTEXT.md
@apps/web/components/chart/NatalWheel.tsx
@apps/web/components/chart/BigThreeCards.tsx
@apps/web/components/chart/ChartView.tsx
@packages/astrology/src/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create placeholder interpretation data</name>
  <files>
    apps/web/lib/interpretations/planets.ts
  </files>
  <action>
Create Bulgarian placeholder interpretations for planets in signs:

1. apps/web/lib/interpretations/planets.ts:

Export function: getPlanetInterpretation(planet: string, sign: string): InterpretationData

InterpretationData type:
- title: string (e.g., "Слънце в Лъв")
- position: string (e.g., "15°23' Лъв, Дом 5")
- brief: string (short trait, 5-10 words)
- placeholder: string (coming soon message)

Placeholder content for each planet (generic, sign-agnostic for now):
- Слънце: "Вашето Слънце показва вашата същност и жизнена сила."
- Луна: "Вашата Луна отразява емоционалния ви свят."
- Меркурий: "Меркурий определя начина, по който мислите и общувате."
- Венера: "Венера показва как обичате и какво цените."
- Марс: "Марс разкрива как действате и се борите."
- Юпитер: "Юпитер показва къде намирате растеж и късмет."
- Сатурн: "Сатурн учи чрез ограничения и отговорности."
- Уран: "Уран носи иновации и неочаквани промени."
- Нептун: "Нептун управлява мечтите и духовността."
- Плутон: "Плутон показва дълбоката трансформация."

Brief traits per sign (hardcoded for now):
```typescript
const SIGN_TRAITS: Record<string, string> = {
  aries: 'енергичен и предприемчив',
  taurus: 'стабилен и практичен',
  gemini: 'любопитен и адаптивен',
  cancer: 'емоционален и грижовен',
  leo: 'харизматичен и щедър',
  virgo: 'аналитичен и прецизен',
  libra: 'дипломатичен и хармоничен',
  scorpio: 'интензивен и проницателен',
  sagittarius: 'оптимистичен и свободолюбив',
  capricorn: 'амбициозен и дисциплиниран',
  aquarius: 'иновативен и хуманен',
  pisces: 'интуитивен и съпричастен',
}
```

Placeholder message for all:
"Пълна интерпретация скоро... В следващото обновление ще получите персонализирано тълкуване, базирано на вашата уникална карта."
  </action>
  <verify>
    - getPlanetInterpretation('sun', 'leo') returns valid InterpretationData
    - All 10 planets have placeholder text
    - All 12 signs have traits
    - All text is in Bulgarian Cyrillic
  </verify>
  <done>Placeholder interpretation data available for all planet/sign combinations</done>
</task>

<task type="auto">
  <name>Task 2: Create PlanetDetail component</name>
  <files>
    apps/web/components/chart/PlanetDetail.tsx
  </files>
  <action>
Create the expandable planet interpretation panel:

1. apps/web/components/chart/PlanetDetail.tsx:
   - 'use client' directive
   - Props: { planet: PlanetPosition | null, onClose: () => void, type?: 'sun'|'moon'|'rising' }

   When planet is null, render nothing (or collapsed state).

   When planet is provided:
   - Glassmorphism card (backdrop-blur, bg-white/5, border)
   - Header: Planet name in Bulgarian + sign
   - Position: degree, sign, house
   - Brief trait from getPlanetInterpretation
   - Placeholder text with "coming soon" styling (slightly muted)
   - Close button (X or "Затвори")

   For Rising/Ascendant (type === 'rising'):
   - Special header: "Възходящ знак"
   - Note if birth time unknown: "приблизителен"

   Animation:
   - Slide in from below or expand in place
   - Use Tailwind transitions or CSS animations
   - Keep simple, no external animation library

   Positioning:
   - Inline below the chart (from CONTEXT.md: "card expands below the chart")
   - Full width on mobile
   - Centered max-width on desktop
  </action>
  <verify>
    - Component renders with planet data
    - Shows Bulgarian planet and sign names
    - Shows degree position
    - Shows placeholder interpretation text
    - Close button works
    - Styling matches glassmorphism theme
  </verify>
  <done>PlanetDetail panel displays planet interpretation with placeholder text</done>
</task>

<task type="auto">
  <name>Task 3: Wire up interactions in ChartView</name>
  <files>
    apps/web/components/chart/NatalWheel.tsx
    apps/web/components/chart/BigThreeCards.tsx
    apps/web/components/chart/ChartView.tsx
  </files>
  <action>
Connect all components for interactive planet selection:

1. Update NatalWheel.tsx:
   - Add selectedPlanet prop to highlight selected planet
   - Visual highlight: ring, glow, or scale transform on selected
   - Ensure click events properly fire onPlanetSelect

2. Update BigThreeCards.tsx:
   - Add selectedType prop to show which is selected
   - Visual highlight on selected card
   - onClick handlers call onSelect with type

3. Update ChartView.tsx:
   - Add state: selectedPlanet (PlanetPosition | null)
   - Add state: selectedType ('sun'|'moon'|'rising' | null)

   Handler flow:
   - When planet clicked in wheel: setSelectedPlanet(planet), setSelectedType(null)
   - When Big Three card clicked: setSelectedType(type), find planet from chart data, setSelectedPlanet(planet)
   - When PlanetDetail closed: setSelectedPlanet(null), setSelectedType(null)

   Layout with PlanetDetail:
   ```
   <div>
     <div className="flex flex-col lg:flex-row">
       <BigThreeCards />
       <NatalWheel />
     </div>
     {selectedPlanet && (
       <PlanetDetail planet={selectedPlanet} type={selectedType} onClose={handleClose} />
     )}
   </div>
   ```

4. Ensure keyboard accessibility:
   - Planet elements should be focusable (tabIndex, role="button")
   - Enter/Space should trigger selection
  </action>
  <verify>
    - Click Sun on wheel -> PlanetDetail shows Sun interpretation
    - Click Moon card in Big Three -> PlanetDetail shows Moon interpretation
    - Selected planet visually highlighted on wheel
    - Selected Big Three card visually highlighted
    - Close button dismisses PlanetDetail
    - Can select different planet to switch detail view
  </verify>
  <done>Complete interactive chart with planet selection and interpretation panels</done>
</task>

</tasks>

<verification>
1. User can tap any of the 10 planets on the wheel (CHART-03)
2. Interpretation panel shows planet position in Bulgarian
3. Interpretation shows placeholder "coming soon" text
4. Big Three cards are tappable with same behavior
5. Selected planet is highlighted on the wheel
6. Panel can be dismissed with close button
7. Unknown birth time shows disclaimer on Rising interpretation
</verification>

<success_criteria>
- User can tap planets to see interpretation (CHART-03)
- All planet positions shown with degrees and sign
- Bulgarian text throughout
- Placeholder clearly indicates full interpretations coming in Phase 5
- Interactions work on both desktop and mobile
- Keyboard accessible
</success_criteria>

<output>
After completion, create `.planning/phases/04-astrology-engine-charts/04-04-SUMMARY.md`
</output>
