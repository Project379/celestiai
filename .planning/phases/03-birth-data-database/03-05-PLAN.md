---
phase: 03-birth-data-database
plan: 05
type: execute
wave: 4
depends_on: ["03-04"]
files_modified:
  - apps/web/app/(protected)/birth-data/page.tsx
  - apps/web/app/(protected)/dashboard/page.tsx
  - apps/web/components/birth-data/BirthDataCard.tsx
  - apps/web/components/birth-data/EditBirthDataDialog.tsx
autonomous: false

must_haves:
  truths:
    - "User can view and edit saved birth data"
    - "Dashboard shows birth data or entry CTA"
    - "No PII sent to analytics or third-party services (SEC-19)"
  artifacts:
    - path: "apps/web/components/birth-data/BirthDataCard.tsx"
      provides: "Display and edit birth data"
      min_lines: 40
    - path: "apps/web/app/(protected)/birth-data/page.tsx"
      provides: "Birth data entry page"
      exports: ["default"]
  key_links:
    - from: "apps/web/app/(protected)/birth-data/page.tsx"
      to: "apps/web/components/birth-data/BirthDataWizard.tsx"
      via: "Renders wizard component"
      pattern: "BirthDataWizard"
    - from: "apps/web/components/birth-data/EditBirthDataDialog.tsx"
      to: "/api/birth-data/[id]"
      via: "PATCH on save"
      pattern: "fetch.*api/birth-data"
---

<objective>
Integrate the birth data wizard into pages, create display/edit components, and verify SEC-19 compliance (no PII sent to third parties).

Purpose: Complete the birth data entry experience with dashboard integration and security verification.
Output: Working birth data page, dashboard integration, edit capability, and SEC-19 verification.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-birth-data-database/03-RESEARCH.md
@.planning/phases/03-birth-data-database/03-CONTEXT.md
@.planning/phases/03-birth-data-database/03-01-SUMMARY.md
@.planning/phases/03-birth-data-database/03-02-SUMMARY.md
@.planning/phases/03-birth-data-database/03-03-SUMMARY.md
@.planning/phases/03-birth-data-database/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create birth data page and dashboard integration</name>
  <files>
    apps/web/app/(protected)/birth-data/page.tsx
    apps/web/app/(protected)/dashboard/page.tsx
    apps/web/components/birth-data/BirthDataCard.tsx
    apps/web/components/birth-data/EditBirthDataDialog.tsx
  </files>
  <action>
    Create the birth data page and integrate with dashboard:

    1. **birth-data/page.tsx**: Entry point for wizard
       - Check if user already has birth data via GET /api/birth-data
       - If no data: Show BirthDataWizard
       - If has data: Redirect to /dashboard or show edit view
       - Bulgarian heading: "Vavedete dannite si za razhdane"
       - Cosmic background consistent with auth pages

    2. **BirthDataCard.tsx**: Display saved birth data
       - Props: chart data object
       - Shows: name, date, time (or approximate range), city
       - Edit button triggers EditBirthDataDialog
       - Minimal display per CONTEXT.md: date, time, city name only
       - Bulgarian labels

    3. **EditBirthDataDialog.tsx**: Inline edit in dialog
       - Native <dialog> element (pattern from Phase 2)
       - Pre-populated form fields from current data
       - Zod validation on save
       - PATCH to /api/birth-data/[id]
       - "Sigurni li ste?" confirmation before save per CONTEXT.md
       - Bulgarian text

    4. **Update dashboard/page.tsx**:
       - Fetch user's birth data on load
       - If no data: Show CTA card linking to /birth-data
         - "Vavedete danni za razhdane" button
       - If has data: Show BirthDataCard
       - Keep existing layout/header from Phase 2

    Use fetch with credentials for API calls. Handle loading and error states.
  </action>
  <verify>
    - /birth-data shows wizard for new users
    - Completing wizard creates chart and redirects to dashboard
    - Dashboard shows birth data card after completion
    - Edit dialog allows updating birth data
    - Edit requires confirmation before saving
  </verify>
  <done>
    Birth data page integrated with dashboard, displaying and editing birth data works
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify SEC-19 compliance - no PII sent to third parties</name>
  <files>
    (verification only - no files modified)
  </files>
  <action>
    Verify that no PII (birth data, coordinates, personal info) is sent to analytics or third-party services:

    1. **Audit codebase for analytics/tracking**:
       - Search for analytics libraries: `grep -r "gtag\|analytics\|posthog\|mixpanel\|segment\|plausible" apps/web/`
       - Search for third-party script tags: `grep -r "<script.*src=" apps/web/`
       - Check for error reporting services: `grep -r "sentry\|bugsnag\|rollbar" apps/web/`

    2. **If analytics/tracking found**, verify PII exclusion:
       - Birth data fields (birthDate, birthTime, cityName, lat, lon) NOT sent
       - Only anonymized events allowed (page views, feature usage counts)
       - No user-identifiable data in event properties

    3. **If NO analytics/tracking found**:
       - Document that no third-party analytics are currently integrated
       - This satisfies SEC-19 by default

    4. **Check API routes**:
       - Verify /api/birth-data does NOT log PII to external services
       - Verify /api/cities/search does NOT send queries to external APIs
       - All data stays within Supabase (first-party)

    5. **Document findings** in verification section below

    SEC-19 is satisfied if:
    - No analytics SDKs are present, OR
    - Analytics are present but PII fields are explicitly excluded from tracking
  </action>
  <verify>
    - grep commands run and output reviewed
    - No PII fields (birth data, coordinates) found in any analytics calls
    - Document: "[PASS/FAIL] SEC-19: No PII sent to analytics - [reason]"
  </verify>
  <done>
    SEC-19 verified: No PII (birth data, coordinates) sent to analytics or third-party services
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete birth data wizard with city search, form validation, dashboard integration, and SEC-19 compliance</what-built>
  <how-to-verify>
    1. Start dev server: `pnpm dev`
    2. Navigate to http://localhost:3000/dashboard
    3. Click "Vavedete danni za razhdane" CTA
    4. Complete wizard:
       - Enter a chart name and birth date
       - Choose known time or approximate range
       - Search for "Sofia" and select from dropdown
       - Review on confirmation step
       - Click "Zapazi"
    5. Verify redirect to dashboard with birth data card
    6. Click edit on the card, modify a field, confirm save
    7. Verify data persists after page refresh

    Expected behaviors:
    - Progress bar advances with each step
    - Back navigation preserves entered data
    - City search shows Sofia at top (city type prioritized)
    - Confirmation shows all entered data clearly
    - Edit dialog asks for confirmation before saving
    - All text is in Bulgarian
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
1. Page integration: /birth-data shows wizard, redirects to dashboard on save
2. Dashboard: Shows CTA or birth data card based on user state
3. Edit flow: Dialog with confirmation pattern from Phase 2
4. SEC-19: Verified no PII sent to third-party services
5. Bulgarian UI: All visible text in Bulgarian
6. Responsive: Works on mobile viewport
</verification>

<success_criteria>
- Birth data page shows wizard for new users
- Dashboard shows birth data or entry CTA based on user state
- User can edit saved birth data with confirmation dialog
- SEC-19 verified: No PII (birth data, coordinates) sent to analytics or third parties
- All UI text in Bulgarian
- Glassmorphism styling consistent with app theme
</success_criteria>

<output>
After completion, create `.planning/phases/03-birth-data-database/03-05-SUMMARY.md`
</output>
