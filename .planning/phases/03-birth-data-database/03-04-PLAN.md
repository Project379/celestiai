---
phase: 03-birth-data-database
plan: 04
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - apps/web/components/birth-data/BirthDataWizard.tsx
  - apps/web/components/birth-data/DateStep.tsx
  - apps/web/components/birth-data/TimeStep.tsx
  - apps/web/components/birth-data/LocationStep.tsx
  - apps/web/components/birth-data/ConfirmStep.tsx
  - apps/web/components/birth-data/CitySearch.tsx
  - apps/web/components/birth-data/BirthDataCard.tsx
  - apps/web/components/birth-data/EditBirthDataDialog.tsx
  - apps/web/app/(protected)/birth-data/page.tsx
  - apps/web/app/(protected)/dashboard/page.tsx
  - apps/web/package.json
autonomous: false

must_haves:
  truths:
    - "User can enter birth date via calendar picker"
    - "User can enter birth time or select 'unknown' with approximate range"
    - "User can search and select Bulgarian city"
    - "User sees preview before saving birth data"
    - "User can view and edit saved birth data"
  artifacts:
    - path: "apps/web/components/birth-data/BirthDataWizard.tsx"
      provides: "Multi-step birth data form"
      min_lines: 80
    - path: "apps/web/components/birth-data/CitySearch.tsx"
      provides: "Bulgarian city autocomplete"
      min_lines: 50
    - path: "apps/web/components/birth-data/BirthDataCard.tsx"
      provides: "Display and edit birth data"
      min_lines: 40
    - path: "apps/web/app/(protected)/birth-data/page.tsx"
      provides: "Birth data entry page"
      exports: ["default"]
  key_links:
    - from: "apps/web/components/birth-data/CitySearch.tsx"
      to: "/api/cities/search"
      via: "fetch in useEffect with debounce"
      pattern: "fetch.*api/cities/search"
    - from: "apps/web/components/birth-data/BirthDataWizard.tsx"
      to: "/api/birth-data"
      via: "POST on form submit"
      pattern: "fetch.*api/birth-data"
    - from: "apps/web/components/birth-data/BirthDataWizard.tsx"
      to: "apps/web/lib/validators/birth-data.ts"
      via: "zodResolver for form validation"
      pattern: "zodResolver.*birthDataSchema"
---

<objective>
Create the birth data wizard UI with step-by-step form, Bulgarian city search, and birth data display/edit components, integrated with the API routes from Plan 03.

Purpose: Enable users to input their birth data through a friendly wizard experience per CONTEXT.md decisions.
Output: Complete birth data entry and editing UI at /birth-data with dashboard integration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-birth-data-database/03-RESEARCH.md
@.planning/phases/03-birth-data-database/03-CONTEXT.md
@.planning/phases/03-birth-data-database/03-01-SUMMARY.md
@.planning/phases/03-birth-data-database/03-02-SUMMARY.md
@.planning/phases/03-birth-data-database/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create birth data wizard with React Hook Form</name>
  <files>
    apps/web/components/birth-data/BirthDataWizard.tsx
    apps/web/components/birth-data/DateStep.tsx
    apps/web/components/birth-data/TimeStep.tsx
    apps/web/components/birth-data/LocationStep.tsx
    apps/web/components/birth-data/ConfirmStep.tsx
    apps/web/package.json
  </files>
  <action>
    Create multi-step wizard following CONTEXT.md decisions and RESEARCH.md patterns:

    1. **Install dependencies**:
       ```bash
       pnpm --filter @celestia/web add react-hook-form @hookform/resolvers
       ```

    2. **BirthDataWizard.tsx**: Main wizard component
       - Use React Hook Form with FormProvider for shared state
       - zodResolver with birthDataSchema from lib/validators/birth-data
       - Steps array: ['date', 'time', 'location', 'confirm']
       - Progress bar showing current step (purple gradient, per CONTEXT.md)
       - Step transitions with currentStep state
       - nextStep/prevStep handlers with per-step validation
       - onSubmit posts to /api/birth-data
       - Success redirects to /dashboard

    3. **DateStep.tsx**: Birth date entry
       - Native date input type="date" (works well, no need for custom calendar)
       - Name field for chart label (e.g., "Моята карта")
       - Bulgarian labels: "Дата на раждане", "Име на картата"
       - "Напред" button with validation check
       - Inline error display per CONTEXT.md

    4. **TimeStep.tsx**: Birth time entry
       - Toggle: birthTimeKnown (checkbox or switch)
       - If known: time input type="time" (HH:MM)
       - If unknown: radio buttons for approximate range
         - morning "Сутрин (06:00-12:00)"
         - afternoon "Следобед (12:00-18:00)"
         - evening "Вечер (18:00-24:00)"
         - night "Нощ (00:00-06:00)"
       - Bulgarian label: "Час на раждане"
       - "Напред" / "Назад" buttons

    5. **LocationStep.tsx**: City selection
       - CitySearch component (Task 2)
       - Manual coordinates toggle with lat/lon inputs
       - Bulgarian labels: "Място на раждане", "Ръчни координати"
       - Selected city display with oblast
       - "Напред" / "Назад" buttons

    6. **ConfirmStep.tsx**: Preview before save
       - Summary card showing all entered data
       - Bulgarian labels: "Преглед на данните"
       - "Изглежда добре?" confirmation text
       - "Запази" (submit) and "Назад" buttons
       - Loading state during submission

    Styling: Glassmorphism cards, cosmic gradients, purple accents per UI-01.
    All text in Bulgarian per UI-02.
  </action>
  <verify>
    - `pnpm typecheck` passes
    - Wizard renders with progress bar
    - Each step validates before allowing "Напред"
    - Form state persists across step navigation
  </verify>
  <done>
    Birth data wizard created with 4 steps, React Hook Form state management, and Bulgarian UI
  </done>
</task>

<task type="auto">
  <name>Task 2: Create city search autocomplete component</name>
  <files>
    apps/web/components/birth-data/CitySearch.tsx
  </files>
  <action>
    Create city search component that calls /api/cities/search:

    1. **CitySearch.tsx**: Autocomplete component
       - Props: onSelect (city) => void, value (current city), error (string)
       - State: query, results, isLoading, isOpen

    2. **Search behavior**:
       - Debounce input by 300ms (use setTimeout pattern)
       - Call /api/cities/search?q={query} on debounced input
       - Show loading spinner during fetch
       - Display dropdown with results

    3. **Results display**:
       - City name with oblast in parentheses: "София (София-град)"
       - City type badge: "град" / "село" / "село"
       - Cities first (per CONTEXT.md), then villages
       - Max 20 results displayed

    4. **Selection**:
       - Click result calls onSelect with full city object
       - Sets form values: cityId, cityName, latitude, longitude
       - Closes dropdown, shows selected city

    5. **Empty/error states**:
       - No results: "Няма намерени резултати"
       - Error: "Грешка при търсене"
       - Min 1 char to search: "Въведете поне 1 символ"

    6. **Keyboard navigation** (optional enhancement):
       - Arrow up/down to navigate results
       - Enter to select highlighted result
       - Escape to close dropdown

    Styling: Dropdown with glassmorphism, purple hover states.
  </action>
  <verify>
    - Typing "соф" shows Sofia and related cities
    - Selecting city populates form fields
    - Debounce prevents excessive API calls
    - Works with both Cyrillic and Latin input
  </verify>
  <done>
    City search autocomplete working with debounce, dropdown, and city type prioritization
  </done>
</task>

<task type="auto">
  <name>Task 3: Create birth data page and dashboard integration</name>
  <files>
    apps/web/app/(protected)/birth-data/page.tsx
    apps/web/app/(protected)/dashboard/page.tsx
    apps/web/components/birth-data/BirthDataCard.tsx
    apps/web/components/birth-data/EditBirthDataDialog.tsx
  </files>
  <action>
    Create the birth data page and integrate with dashboard:

    1. **birth-data/page.tsx**: Entry point for wizard
       - Check if user already has birth data via GET /api/birth-data
       - If no data: Show BirthDataWizard
       - If has data: Redirect to /dashboard or show edit view
       - Bulgarian heading: "Въведете данните си за раждане"
       - Cosmic background consistent with auth pages

    2. **BirthDataCard.tsx**: Display saved birth data
       - Props: chart data object
       - Shows: name, date, time (or approximate range), city
       - Edit button triggers EditBirthDataDialog
       - Minimal display per CONTEXT.md: date, time, city name only
       - Bulgarian labels

    3. **EditBirthDataDialog.tsx**: Inline edit in dialog
       - Native <dialog> element (pattern from Phase 2)
       - Pre-populated form fields from current data
       - Zod validation on save
       - PATCH to /api/birth-data/[id]
       - "Сигурни ли сте?" confirmation before save per CONTEXT.md
       - Bulgarian text

    4. **Update dashboard/page.tsx**:
       - Fetch user's birth data on load
       - If no data: Show CTA card linking to /birth-data
         - "Въведете данни за раждане" button
       - If has data: Show BirthDataCard
       - Keep existing layout/header from Phase 2

    Use fetch with credentials for API calls. Handle loading and error states.
  </action>
  <verify>
    - /birth-data shows wizard for new users
    - Completing wizard creates chart and redirects to dashboard
    - Dashboard shows birth data card after completion
    - Edit dialog allows updating birth data
    - Edit requires confirmation before saving
  </verify>
  <done>
    Birth data page integrated with dashboard, displaying and editing birth data works
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete birth data wizard with city search, form validation, and dashboard integration</what-built>
  <how-to-verify>
    1. Start dev server: `pnpm dev`
    2. Navigate to http://localhost:3000/dashboard
    3. Click "Въведете данни за раждане" CTA
    4. Complete wizard:
       - Enter a chart name and birth date
       - Choose known time or approximate range
       - Search for "София" and select from dropdown
       - Review on confirmation step
       - Click "Запази"
    5. Verify redirect to dashboard with birth data card
    6. Click edit on the card, modify a field, confirm save
    7. Verify data persists after page refresh

    Expected behaviors:
    - Progress bar advances with each step
    - Back navigation preserves entered data
    - City search shows Sofia at top (city type prioritized)
    - Confirmation shows all entered data clearly
    - Edit dialog asks for confirmation before saving
    - All text is in Bulgarian
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
1. Wizard flow: All 4 steps work with validation
2. City search: Returns results with coordinates
3. Form state: Data persists across step navigation
4. API integration: Creates/updates via /api/birth-data
5. Dashboard: Shows CTA or birth data card based on user state
6. Edit flow: Dialog with confirmation pattern from Phase 2
7. Bulgarian UI: All visible text in Bulgarian
8. Responsive: Works on mobile viewport
</verification>

<success_criteria>
- User can enter birth date, time (or unknown with range), and city
- City search shows Bulgarian cities with prioritization
- Wizard has progress bar and back navigation
- Data saves to database via API
- Dashboard shows birth data or entry CTA
- User can edit saved birth data with confirmation
- All UI text in Bulgarian
- Glassmorphism styling consistent with app theme
</success_criteria>

<output>
After completion, create `.planning/phases/03-birth-data-database/03-04-SUMMARY.md`
</output>
