---
phase: 03-birth-data-database
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - packages/db/src/seed/cities.ts
  - packages/db/src/seed/data/bulgarian-cities.json
  - packages/db/package.json
  - apps/web/app/api/cities/search/route.ts
autonomous: true

must_haves:
  truths:
    - "City search returns Bulgarian cities with coordinates"
    - "Search prioritizes cities over villages"
    - "Results include latitude and longitude for each match"
  artifacts:
    - path: "packages/db/src/seed/data/bulgarian-cities.json"
      provides: "Bulgarian city data with coordinates"
      min_lines: 100
    - path: "apps/web/app/api/cities/search/route.ts"
      provides: "City search API endpoint"
      exports: ["GET"]
  key_links:
    - from: "apps/web/app/api/cities/search/route.ts"
      to: "packages/db/src/schema/cities.ts"
      via: "Drizzle query"
      pattern: "cities"
---

<objective>
Seed Bulgarian city data into the cities table and create a search API endpoint that returns cities with coordinates for the birth data form location step.

Purpose: Enable the location search in the birth data wizard with accurate Bulgarian city coordinates.
Output: Seeded city data and working /api/cities/search endpoint.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-birth-data-database/03-RESEARCH.md
@.planning/phases/03-birth-data-database/03-CONTEXT.md
@.planning/phases/03-birth-data-database/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Bulgarian city seed data</name>
  <files>
    packages/db/src/seed/data/bulgarian-cities.json
    packages/db/src/seed/cities.ts
    packages/db/package.json
  </files>
  <action>
    Create seed data and seeding script for Bulgarian cities:

    1. **bulgarian-cities.json**: Create JSON file with Bulgarian settlements. Use SimpleMaps data (MIT license) as the primary source. Include at minimum:
       - All 28 oblast capitals (Sofia, Plovdiv, Varna, Burgas, Ruse, Stara Zagora, Pleven, Sliven, Dobrich, Shumen, Pernik, Haskovo, Yambol, Pazardzhik, Blagoevgrad, Veliko Tarnovo, Vratsa, Gabrovo, Vidin, Montana, Lovech, Razgrad, Targovishte, Silistra, Kardzhali, Smolyan, Kyustendil, Sofia Province)
       - Major cities (population > 10,000)
       - Towns (population 5,000-10,000)
       - Common villages (population > 2,000)

       Each entry should have:
       ```json
       {
         "name": "София",
         "nameAscii": "Sofia",
         "oblast": "София-град",
         "ekatte": "68134",
         "type": "city",
         "latitude": 42.6977,
         "longitude": 23.3219,
         "population": 1307000
       }
       ```

    2. **cities.ts**: Create seeding script:
       ```typescript
       import { db } from '../client'
       import { cities } from '../schema'
       import data from './data/bulgarian-cities.json'

       export async function seedCities() {
         console.log('Seeding Bulgarian cities...')
         await db.insert(cities).values(data).onConflictDoNothing()
         console.log(`Seeded ${data.length} cities`)
       }
       ```

    3. Add `"seed": "tsx src/seed/cities.ts"` script to package.json.

    Include at least 150+ settlements covering major population centers. Prioritize accuracy of coordinates over comprehensiveness.
  </action>
  <verify>
    - JSON file parses without errors
    - All entries have required fields (name, nameAscii, oblast, type, lat, lon)
    - Coordinates are within Bulgaria bounds (41.2-44.2 lat, 22.4-28.6 lon)
  </verify>
  <done>
    Bulgarian city data file ready with 150+ settlements and accurate coordinates
  </done>
</task>

<task type="auto">
  <name>Task 2: Create city search API endpoint</name>
  <files>
    apps/web/app/api/cities/search/route.ts
  </files>
  <action>
    Create GET endpoint for city search at `/api/cities/search`:

    1. Accept query params: `q` (search term), `limit` (optional, default 20)

    2. Validate with Zod:
       - q: string, min 1, max 100
       - limit: number, min 1, max 50, default 20

    3. Search logic:
       - Match against both `name` (Bulgarian) and `nameAscii` (Latin) using ILIKE
       - Order results: cities first, then towns, then villages (CASE WHEN type...)
       - Secondary sort by name alphabetically
       - Limit results

    4. Return JSON array with: id, name, oblast, type, latitude, longitude

    5. Protect route with `auth.protect()` per SEC-17 pattern from Phase 2.

    Example response:
    ```json
    [
      { "id": "...", "name": "София", "oblast": "София-град", "type": "city", "latitude": 42.6977, "longitude": 23.3219 },
      { "id": "...", "name": "Софийски манастир", "oblast": "София", "type": "village", ... }
    ]
    ```

    Return 400 for invalid query, empty array for no matches.
  </action>
  <verify>
    - `curl -H "Authorization: Bearer $TOKEN" "http://localhost:3000/api/cities/search?q=соф"` returns Sofia and related cities
    - Response includes coordinates
    - Cities appear before villages in results
    - Unauthenticated requests return 401/404
  </verify>
  <done>
    City search API returns Bulgarian cities with coordinates, prioritized by type
  </done>
</task>

</tasks>

<verification>
1. Seed data: `packages/db/src/seed/data/bulgarian-cities.json` has 150+ entries
2. Coordinates valid: All lat/lon within Bulgaria bounds
3. API works: `/api/cities/search?q=пловдив` returns Plovdiv as first result
4. Auth protected: Unauthenticated requests fail
5. Type ordering: Cities before towns before villages
</verification>

<success_criteria>
- Bulgarian city data seeded with accurate coordinates
- Search API returns relevant results with lat/lon
- Results prioritize cities over smaller settlements
- API protected with Clerk authentication
- Search works with both Cyrillic and Latin input
</success_criteria>

<output>
After completion, create `.planning/phases/03-birth-data-database/03-02-SUMMARY.md`
</output>
