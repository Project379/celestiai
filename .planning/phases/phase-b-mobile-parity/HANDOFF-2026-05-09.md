# Phase B Handoff (2026-05-09) — Mobile Parity + Кръг

**Created:** 2026-05-09 at Phase B opening, after the soft-launch quality bar reframe.
**Supersedes:** `.planning/PHASE-A-CLOSE-RATIFICATION.md` (deleted; relevant content migrated here).

This document captures Phase B's execution shape, the strategic items that gate sub-rounds, and the open coordination dependencies. Read this and `MOBILE-WEB-PARITY-GAP.md` as a pair before opening any Stream P sub-round.

---

## Phase B framing — two streams

Per founder ratification 2026-05-09:

- **Stream P (Parity).** Port all missing web features to mobile per `MOBILE-WEB-PARITY-GAP.md`. Toni's primary work for the duration of Phase B.
- **Stream K (Кръг).** Port Кръг features as friend ships them on web. Fires reactively per the **tiered interleaving rules below**; per-port investigation pass when each feature lands.

**Soft-launch quality bar at end of Phase B = full web parity less Friends groups (deferred to future research).** TestFlight cut at end of Phase B remains; timeline is parity-work-driven, no anxiety on duration.

Phase B closes when the parity gap inventory is fully resolved (`done` or `deferred` on every row in `MOBILE-WEB-PARITY-GAP.md`) AND the Кръг scope ships through Stream K (less Friends groups, less Tier 3 deferrals) AND SR 9 EAS+TestFlight provisioning lands.

### Stream K interleaving — tiered priority (ratified 2026-05-09)

Friend's web Кръг shipping order is unknown ahead of time. Stream K firing rules are now tiered, not uniform halt-and-port:

- **Tier 1 — synastry.** Drives RevenueCat sandbox path + Кръг tab core feature. **When friend ships, Claude Code halts at next sub-round close, surfaces "Tier 1 K port available, recommend halt of Stream P", founder ratifies the cut-in.** Stream P resumes at the next P.x sub-round after Stream K port closes.
- **Tier 2 — «Днешен ден в твоя кръг».** Premium daily feature, not gating other work. **Queue for next natural break in Stream P, port within current Stream P phase.** Doesn't trigger an immediate halt; lands at a Stream P sub-round boundary that's a clean cut-in point.
- **Tier 3 — crush reports, couples linked, yearly forecast.** Less time-sensitive. **Defer to a dedicated Stream K phase OR Phase C.** Don't interrupt Stream P.
- **NOT scope — Friends groups.** Deferred to future research per Phase B opening ratification.

Tier 2/3 ports queue in `STREAM-K-PORT-LOG.md` (created when first Кръг port lands) with the decision rationale for tier assignment. If friend's actual shipping changes a feature's priority during execution, founder re-tiers at the touchpoint.

---

## Strategic items (carry-forward from Phase A close)

The five strategic items ratified at Phase A close still need committed timelines. Status updated 2026-05-09 to reflect Phase B re-plan.

### 1. Cost envelope check on OpenRouter

**Owner:** founder
**Target window:** complete before SR 9 fires (Phase B close)
**Status:** not started

Cross-references `PRE_LAUNCH_PREREQS.md` item 5. Two-state projection: current Llama-only state + projected mixed-model state (Gemini for Oracle planned in Phase B, Llama stays on daily horoscope where speed matters). Set OpenRouter spend alerts in dashboard. Resolve `PRE_LAUNCH_PREREQS.md` item 5a (AI provider fallback strategy) — graceful degradation vs second-provider failover.

Now that Phase B is full-parity-bar, the cost projection should also account for the additional AI surfaces being ported (e.g., Oracle streaming throughput, daily-horoscope streaming throughput) — these affect token throughput patterns, not call counts.

**Success criterion:** projection note exists in `PRE_LAUNCH_PREREQS.md` item 5 confirming spend fits the founder's pre-launch burn ceiling. OpenRouter spend alerts active.

### 2. Telemetry vendor decision — PostHog selected

**Owner:** founder + Toni (wiring lands in Stream P)
**Target window:** wire in P.13 (recommended early in Stream P)
**Status:** vendor selected (PostHog), wiring deferred

Locked at Phase A close ratification — **PostHog**, EU region, web + mobile to the same project. 10-event taxonomy locked:

| # | Event | Properties | Surface |
|---|---|---|---|
| 1 | `app_opened` | `platform: ios|android|web`, `cold_start: bool` | both |
| 2 | `wizard_started` | `platform` | both |
| 3 | `wizard_step_completed` | `step: 'date'|'time'|'location'|'confirm'`, `platform` | both |
| 4 | `wizard_completed` | `platform`, `time_known: bool` | both |
| 5 | `chart_viewed` | `platform`, `is_first_view: bool` | both |
| 6 | `oracle_opened` | `platform`, `has_chart: bool` | both |
| 7 | `oracle_topic_selected` | `topic`, `cache_hit: bool`, `platform` | both |
| 8 | `oracle_reading_generated` | `topic`, `tier: 'free'|'premium'`, `platform` | both |
| 9 | `oracle_cap_reached` | `cap`, `tier`, `platform` | both |
| 10 | `push_permission_prompted` + `push_permission_response` | `granted: bool`, `platform: ios|android` | mobile-only |

Defer richer events until questions arise. Bulgarian Privacy Policy must include PostHog as data processor (gates GDPR/ToS item below).

**Success criterion:** PostHog project exists in EU region. Stream P wires the 10-event taxonomy on both surfaces. First end-to-end signup funnel event (`app_opened` → `wizard_started` → ... → `chart_viewed`) lands in the dashboard.

### 3. Apple Developer Program enrollment

**Owner:** founder
**Target window:** begin within 2 weeks of Phase A close (by 2026-05-23); complete before SR 9 fires (now end of Phase B per the soft-launch reclassification, timeline TBD based on Stream P duration)
**Status:** not started; pending decision on individual vs organization account

| Path | Cost | Lead time | Tradeoff |
|---|---|---|---|
| Individual | $99/year | ~1 week (instant for many applicants) | App listed under founder's personal name |
| Organization | $99/year + D-U-N-S number | ~1–2 weeks for D-U-N-S lookup + ~1 week for Apple review | App listed under company name; required if regulated data or team adds |

For a Bulgarian astrology app at soft-launch scale (50–100 users), **individual is sufficient**. Organization makes sense if a registered company entity already exists, founder plans to add team members in Phase C–D, or Bulgarian data-protection regulation requires the developer entity to match the data controller named in the Privacy Policy.

**Apple Developer team ID + RevenueCat App Store product IDs available before SR 9 (P.17) planning fires.** Otherwise SR 9 stalls and the soft-launch milestone slips.

### 4. GDPR/ToS engagement

**Owner:** founder + legal review
**Target window:** Bulgarian Privacy Policy + ToS draft live before TestFlight external testing opens (Apple requirement)
**Status:** privacy draft exists at `.planning/legal/pending-review/privacy-draft.md` (discovered in survey 2026-05-09); not engaged with legal yet

Apple requires a Privacy Policy URL for external TestFlight testing — gates Phase B soft launch, not SR 9 internal build. Internal TestFlight (founder + close circle) doesn't need it; external TestFlight (the 50–100 Bulgarian users invited to soft launch) does.

**Required artifacts:**

- **Bulgarian Privacy Policy** (draft exists; needs legal review + Bulgarian native-speaker review for register/calibration). Discloses every processor:
  - Clerk (auth + 2FA + OAuth)
  - Supabase (database + storage)
  - OpenRouter + underlying model providers (Llama / Gemini)
  - Stripe (web payments)
  - Sentry (error monitoring — EU region)
  - **RevenueCat** (mobile payments — Stream P P.15)
  - **PostHog** (analytics — Stream P P.13)
  - **Apple Push Notification Service** (push delivery — Stream P P.16)
- **Bulgarian Terms of Service** — drafted in Bulgarian, native-speaker legal review
- **Processor contracts (DPAs)** — Data Processing Agreements with each of the eight processors above
- **Cookie consent banner on web** — only required if PostHog uses tracking cookies; cookieless mode (localStorage) avoids the banner. Founder picks at PostHog config time

### 5. Friend coordination on web premium features — **CLOSED 2026-05-09**

**Owner:** founder
**Target window:** scheduled and completed 2026-05-09
**Status:** **closed — no blocks.** Friend on a separate branch with separate workstream (premium features). origin/main has prior shipped work (CA-0001 Llama LLM, CA-0002 Stripe trial + db improvements, chart polish, settings UX). The CA-0002 vs `packages/db` retirement divergence routed to B.0c re-implementation rather than direct merge. Friend agreed to discard CA-0002 branch; re-implementation by Toni in canonical architecture.

Survey 2026-05-09 finding: `git diff origin/main..HEAD apps/web/` returned empty — friend's CA-0001 (Llama LLM) and CA-0002 (Stripe trial period + database improvements) appear to either already be in `mobile-parallel-test` or never landed on `origin/main`. **`develop` branch was unfrozen** (memory said frozen historical reference; it appeared as a "new branch" in the fetch). 30-second clarification needed at the friend conversation.

The friend conversation is also needed for Stream K planning — friend's web shipping order dictates Stream K sub-round sequence.

**Action items:**

- Founder schedules friend conversation within 1 week (by 2026-05-16)
- Surface findings to this doc:
  - Friend's branch state — what's the actual state of `develop` and `origin/main` post-CA-0001/0002?
  - In-flight web work — what's happening on the web product, especially Кръг features
  - Merge plan — straight merge, rebase, or branch-by-branch reconciliation if any web changes are missing from `mobile-parallel-test`
  - Coordination cadence — does friend need read access to `mobile-parallel-test` for cross-surface awareness, or periodic syncs sufficient
- Friend's web Кръг shipping order drives Stream K sub-round sequence

**Success criterion:** a short note appended to this file capturing friend's branch state, in-flight web work, agreed merge plan with target date, ongoing coordination cadence, and Stream K shipping order expectations.

---

## Stream P sub-round layering (ratified 2026-05-09)

Eighteen sub-rounds, locked in execution order below. Total estimate ~6,000 LOC + 1 Supabase migration (push_tokens) + EAS config. Per-sub-round investigation passes refine the within-sub-round scope before commits.

| SR | Scope | Gap items covered | LOC est. | Notes |
|---|---|---|---|---|
| **B.0** | Friend coordination merge | n/a | n/a | **CLOSED 2026-05-09.** Friend confirmed no blocks; on a separate branch with separate workstream (premium features). origin/main has CA-0002 (Stripe trial + db improvements) — surfaced architectural divergence with mobile-parallel-test's `packages/db` retirement (Strategy C kill-Drizzle, commit 52bb5e1), routed to B.0c re-implementation rather than direct merge. |
| **B.0d** | RLS security lockdown — production Supabase | n/a | 11 ALTER TABLE + 6 CREATE POLICY (founder ran via Supabase SQL Editor) | **CLOSED 2026-05-09.** Audit found 12/17 public-schema tables with RLS disabled; publishable key had full anon CRUD on `users`, `audit_logs`, `chart_calculations`, `processed_webhook_events`, `user_crystals`, `user_daily_crystals`, `daily_transits`. Lockdown applied per INTERNAL/USER_DATA/CATALOG taxonomy. 10/10 verification curls pass post-lockdown. App smoke-test confirmed authed flows still work. Pollution rows from anon-write tests cleaned. Full write-up: `.planning/INCIDENT-2026-05-09-RLS-EXPOSURE.md`. Canonical posture per table: `.planning/SECURITY-MODEL.md`. |
| **B.0e** | server.ts JWT fallback audit + public.ts cleanup | n/a | TBD per investigation | **QUEUED NEXT.** REVISIT-30. `apps/web/lib/supabase/server.ts` falls back to default Clerk token when 'supabase' template absent — need to confirm whether the Clerk dashboard still has the 'supabase' template or whether the fallback path silently sends unverifiable tokens. Also remove `console.log` debug artifacts from `apps/web/lib/supabase/public.ts`. Investigation-pass-first per speed-mode discipline. |
| **B.0c** | CA-0002 re-implementation in canonical architecture | new web libraries (~310 LOC ensure-user + auth/guards) + stripe handler rewrite + 2 supabase/migrations files marked-applied | ~1,500 (Option A scope ratified) | **QUEUED AFTER B.0e.** Investigation pass already done; awaiting ratification + execution. Re-implements CA-0002's Stripe trial period flow + AppUser canonical layer + schema-hardening migration history hygiene against `supabase/migrations/` (no Drizzle, per Strategy C). Founder operational step: `supabase migration repair --status applied <timestamps>` after the migration files land so Supabase doesn't re-run them. |
| **P.1** | Днес parity batch (absorbs original P.14 real bento data layer) | Section 1 items 1.1–1.10 | ~600 | First parity sub-round. Largest single tab batch. Item 1.4 animated sun sigil flagged for D8 budget concern (recalibrated). |
| **P.2** | Карта parity batch | Section 2 items 2.1–2.7 | ~700 | AstrologyReference is heaviest sub-component. Item 2.7 wheel arrival animation: D8 budget recalibrated to ~150–250 LOC. |
| **P.4** | Lunar diary route — `/rhythm/journal` | Section 4 item 4.7 | ~800 | Largest single sub-round. Ships BEFORE P.3 so the Ритъм CTA card has a working link at landing. |
| **P.3** | Ритъм parity batch | Section 4 items 4.1–4.6 | ~400 | Diary CTA card now real link. |
| **P.5** | Ти tab section routing + dynamic Big Three subtitle (stub destinations until P.6/7/8/10/11 land) | Section 5 items 5.1, 5.8 | ~150 | Premium route stubs to P.11 destination. |
| **P.7** | `/you/recommendations` (stories catalog) | Section 5 item 5.3 | ~500 | AsyncStorage state per D7. **REVISIT-28 logged for cross-device sync.** P.7 investigation surfaces in-app notice decision on silent drift. |
| **P.6** | `/you/crystals` (monthly windows + daily streak + collection) | Section 5 item 5.2 | ~400 | All three views per D9. |
| **P.8** | `/you/guide` (full astrology reference) | Section 5 item 5.4 | ~600 | Largest editorial port. |
| **P.10** | Account settings (`<UserProfile>` Clerk RN + custom app-specific) | Section 5 item 5.7 | ~400 | Independent of RevenueCat. GDPR export trigger + account deletion confirm here. |
| **P.9** | Subscription management UI (read-only initially; full cancel/reactivate when P.15 lands) | Section 5 item 5.6, Section 7 item 7.1 | ~300 | Platform-native subscription manager deep link. |
| **P.15** | RevenueCat install + paywall integration (REVISIT-25 lift) | enables P.11 + P.9 cancel/reactivate | ~300 | Halt risk: sandbox testing needs Apple Developer enrollment. |
| **P.11** | Pricing surface — DUAL: inline `<MobilePaywall>` + Ти→Премиум destination | Section 7 item 7.2, Section 5 row 5.9 (added) | ~300 (was ~250 + ~50 for destination) | Mobile pattern differs from web `/pricing`. P.5 stubbed the Премиум route; P.11 builds destination content. |
| **P.12** | Oracle parity polish (streaming + sentinel colors + animated loading + regenerate) | Section 6 items 6.1, 6.2, 6.4, 6.5 | ~300 | All four polish items in one sub-round. |
| **P.16** | Push delivery infra (REVISIT-26 lift) | Section 7 item 7.4 | ~250 | 1 Supabase migration: `push_tokens`. End-to-end delivery testable post-P.17. |
| **P.13** | PostHog telemetry wiring (10-event taxonomy from §13.2 above) | n/a — instrumentation | ~150 | **Moved from #2 to second-to-last per founder ratification.** Wires after all surfaces exist; soft-launch users generate the meaningful data. **REVISIT-29 logged for telemetry expansion to new surfaces in Phase C.** |
| **P.17** | SR 9 — EAS Dev Client + TestFlight provisioning + biometric auth (REVISIT-1 + REVISIT-27 close) | gates soft launch | ~50 LOC + EAS config + biometric | Apple Developer team ID required (founder track). |
| **P.18** | Soft launch readiness verification | docs only | n/a | Phase B close. |

### Revised execution order (ratified 2026-05-09; updated post-B.0d)

```
B.0 ✓  →  B.0d ✓  →  B.0e  →  B.0c  →  P.1  →  P.2  →  P.4  →  P.3  →  P.5  →  P.7  →  P.6  →  P.8  →  P.10  →  P.9  →  P.15  →  P.11  →  P.12  →  P.16  →  P.13  →  P.17  →  P.18
```

✓ = closed. B.0 (friend coordination) and B.0d (RLS lockdown) are done. B.0e (server.ts JWT fallback audit + public.ts cleanup) fires next; B.0c (CA-0002 re-implementation) follows; P.1 is the first parity sub-round.

P.13 PostHog stays at #16 (second-to-last) per prior ratification. Rationale: wiring 10 events early would lock taxonomy against surfaces that don't exist yet; new mobile surfaces (diary, recommendations, crystals collection, settings) ship without event coverage. Better to wire telemetry once all surfaces exist, expand event taxonomy at that point if needed, ship soft launch with comprehensive event coverage. Internal-usage telemetry data during Stream P has no value — only soft-launch users (post-P.17) generate meaningful data.

Sub-rounds fire sequentially per this order. Stream K cut-ins follow the tiered interleaving rules above.

### Locked decisions (ratified 2026-05-09)

- **D1** universal-first per-component, default platform-specific where framer-motion is involved. Lift only data + Bulgarian editorial copy.
- **D2** Bulgarian copy 80% mirror / 20% calibrate, per-sub-round investigation pass, batched bulgarian-skill invocation at each sub-round close.
- **D3** Pricing surface ships DUAL — inline `<MobilePaywall>` component + Ти→Премиум destination. P.11 builds both.
- **D4** Subscription management — platform-native deep link + RevenueCat-driven cancel/reactivate UI when P.15 lands.
- **D5** Account settings — Clerk RN `<UserProfile>` + custom app-specific section.
- **D6** Lunar diary — same Supabase backend, mobile UI-only port. No schema changes. Cross-surface sync automatic.
- **D7** Recommendations — AsyncStorage parity for ship velocity. **REVISIT-28 filed** for cross-device sync at Phase C/D revisit.
- **D8** Wheel arrival animation in P.2.7 — full Reanimated 4 port required for parity. **Budget recalibrated to ~150–250 LOC** (NOT ~50). Same concern applies to P.1.4 sun sigil. If either balloons, surface via budget_overrun_discipline; may warrant standalone polish sub-round if both balloon together.
- **D9** Crystals collection — full port (monthly windows + daily streak + collection view).
- **D10** PostHog locked at 10 events through Stream P. **REVISIT-29 filed** for taxonomy expansion to new mobile surfaces 4 weeks post-soft-launch.
- **D11** Stream K tiered interleaving (synastry Tier 1 / «Днешен ден» Tier 2 / crush+couples+yearly Tier 3 / Friends NOT scope).
- **D12** P.17 fires only after Apple Developer enrollment confirmed; not gated to a Stream P milestone otherwise.

---

## Phase B → Stream P sub-round opening checklist

Before opening any Stream P sub-round, confirm:

- [ ] **Friend coordination held** — branch state known, no surprise merges pending (gates Stream P start; item 5 above)
- [ ] **PostHog project created** in EU region (item 2; can land in Stream P P.13 even if not pre-created)
- [ ] **Apple Developer enrollment in motion** — individual vs organization decided; team ID expected within 1–2 weeks of P.17 planning (item 3; gates only P.17 specifically)
- [ ] **GDPR/ToS engagement scheduled** — Bulgarian-language lawyer engaged (or scheduled) before external TestFlight (item 4; gates only the soft-launch external invite, not Stream P sub-rounds individually)
- [ ] **Cost envelope baseline pulled** — OpenRouter dashboard data for the past 3 weeks (item 1; parallel founder track, doesn't gate sub-rounds individually)

Item 5 (friend coordination) is closed. Stream P proceeds through the B.0d/B.0e/B.0c sub-round chain before P.1 fires.

---

## Founder reference notes (carry-forward from Phase A close)

- **Reclassification ambiguity flagged at Phase A close:** Founder referenced "REVISIT-23 (TestFlight)" in the close ratification spec. The actual REVISIT-23 in the file is "Web Oracle cap-reached path fails silently" (web parity gap). REVISIT-1 covers Apple Developer enrollment + EAS Dev Client + TestFlight as a bundled item. Both possible interpretations were applied — REVISIT-1 trigger updated to end-of-Phase-B, REVISIT-23 trigger pulled forward to Phase B middle weeks. Now that Phase B is full-parity-bar, REVISIT-23 falls naturally into Stream P scope (Section 8 of the gap inventory). Founder confirms or corrects at next coordination touchpoint.
- **Bulgarian copy discipline preserved across all docs:** Planning docs are English (internal); user-facing strings are Bulgarian (calibrated). No drift introduced in the Phase A close sweep or this Phase B opener.

---

## Document map (where to look)

| Doc | Purpose |
|---|---|
| `MOBILE-WEB-PARITY-GAP.md` (this directory) | Stream P single source of truth; surface-by-surface gap items with status tracking |
| `HANDOFF-2026-05-09.md` (this file) | Phase B opening, two streams, strategic items, friend coordination gating |
| `STREAM-K-PORT-LOG.md` (this directory, created when first Кръг port lands) | Per-port investigation log for Кръг ports as friend ships features |
| `.planning/PROJECT.md` | High-level milestone framing (v0.1 web complete, v1.0 mobile-led launch in progress) |
| `.planning/ROADMAP.md` | Phase A historical + Phase B framing |
| `.planning/STATE.md` | Current position snapshot |
| `.planning/phases/phase-a-mobile-scaffold/SUB-ROUND-{1..8}-CLOSE.md` | Phase A historical record |
| `.planning/phases/phase-a-mobile-scaffold/REVISIT-TRIGGERS.md` | All REVISIT items with current trigger conditions |
| `.planning/phases/phase-a-mobile-scaffold/HANDOFF-2026-05-08.md` | Phase A handoff snapshot (historical; useful for SR 1-8 trail) |

---

## Branch state at Phase B opening

`mobile-parallel-test` at `a796d12` (Phase A close planning sweep), pushed to origin. Working tree clean of mobile/planning changes (only the long-standing `.claude/settings*` and `.planning/phases/m3-uat/RESULTS.json` and untracked `.agent/` / `ecosystem.config.cjs` / `start-worker.js` remain — none of those are Phase B scope).

This commit (Phase B opener docs) lands at `<commit hash TBD>`.

---

*Document generated 2026-05-09 at Phase B opening. Updated as strategic items resolve and Stream P sub-rounds close.*
