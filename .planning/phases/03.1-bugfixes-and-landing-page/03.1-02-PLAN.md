---
phase: 03.1-bugfixes-and-landing-page
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/components/auth/UserMenu.tsx
autonomous: true

must_haves:
  truths:
    - "Only one logout option is visible in the UserButton dropdown"
    - "The custom logout with confirmation dialog still works"
    - "All visible UI text is in Bulgarian Cyrillic"
  artifacts:
    - path: "apps/web/components/auth/UserMenu.tsx"
      provides: "UserButton with hidden default sign out"
      contains: "showSignOutButton"
  key_links:
    - from: "apps/web/components/auth/UserMenu.tsx"
      to: "LogoutConfirmDialog"
      via: "custom UserButton.Action"
      pattern: "UserButton.Action.*label.*Изход"
---

<objective>
Fix the duplicate logout button issue in the user menu.

Purpose: BUG-01 reports two logout buttons visible. The Clerk UserButton shows its built-in "Sign out" option by default, plus we added a custom logout action with confirmation dialog. Need to hide the default one.

Output: Single logout option that triggers the confirmation dialog.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/web/components/auth/UserMenu.tsx
@apps/web/components/auth/LogoutConfirmDialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hide default sign out button in UserButton</name>
  <files>apps/web/components/auth/UserMenu.tsx</files>
  <action>
The Clerk UserButton has a `showSignOutButton` property that can be set to false to hide the default sign out option.

Update the UserButton component to add `showSignOutButton={false}`:

```tsx
<UserButton
  showSignOutButton={false}  // Add this prop to hide default sign out
  appearance={{
    elements: {
      avatarBox: 'h-9 w-9 ring-2 ring-purple-500/50 hover:ring-purple-400/70 transition-all',
      userButtonPopoverCard: 'bg-slate-900/95 backdrop-blur-xl border border-slate-700/50 shadow-xl',
      userButtonPopoverActionButton: 'text-slate-300 hover:text-slate-100 hover:bg-slate-800/50',
      userButtonPopoverActionButtonText: 'text-slate-300',
      userButtonPopoverActionButtonIcon: 'text-slate-400',
      userButtonPopoverFooter: 'hidden',
    },
  }}
>
```

This hides the built-in "Sign out" button while keeping our custom "Изход" action that triggers the confirmation dialog.
  </action>
  <verify>
Read the file and confirm `showSignOutButton={false}` is present on the UserButton component.
Run TypeScript check: `cd apps/web && npx tsc --noEmit`
  </verify>
  <done>
Only one logout option visible (the custom one with confirmation dialog).
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit for remaining Latin text in components</name>
  <files>
    apps/web/components/auth/UserMenu.tsx
    apps/web/components/auth/LogoutConfirmDialog.tsx
    apps/web/components/birth-data/*.tsx
    apps/web/components/dashboard/*.tsx
  </files>
  <action>
Search for any remaining English/Latin text that should be Bulgarian Cyrillic.

Check these files for user-facing text strings:
1. UserMenu.tsx - Already uses "Изход" (correct)
2. LogoutConfirmDialog.tsx - Check all button labels and messages
3. BirthDataWizard.tsx and step components - Check all labels
4. DashboardContent.tsx - Check all text

Previous commits fixed many Latin text issues. Verify no regressions and fix any remaining issues.

Common patterns to look for:
- Button labels (Submit, Cancel, Next, Back, Save, Edit, Delete)
- Error messages
- Loading states
- Placeholder text
- Validation messages

All user-facing text must be in Bulgarian Cyrillic per UI-02.

If any Latin text is found, convert to Bulgarian Cyrillic:
- "Loading" -> "Зареждане"
- "Submit" -> "Изпрати"
- "Cancel" -> "Отказ"
- "Next" -> "Напред"
- "Back" -> "Назад"
- "Save" -> "Запази"
- "Edit" -> "Редактирай"
- "Delete" -> "Изтрий"
- "Error" -> "Грешка"
  </action>
  <verify>
Grep for common English words in TSX files to confirm no Latin UI text remains.
Run: `grep -rn "Loading\|Submit\|Cancel\|Next\|Back\|Save\|Edit\|Delete\|Error" apps/web/components --include="*.tsx" | grep -v "// " | grep -v console`
  </verify>
  <done>
All user-visible text in components is in Bulgarian Cyrillic.
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `cd apps/web && npx tsc --noEmit`
2. UserButton has showSignOutButton={false}
3. No English UI text in component files (code comments excluded)
</verification>

<success_criteria>
- [ ] Only one logout option visible in UserButton dropdown
- [ ] Custom logout triggers confirmation dialog
- [ ] All button labels are in Bulgarian Cyrillic
- [ ] All error messages are in Bulgarian Cyrillic
- [ ] All loading states show Bulgarian text
- [ ] TypeScript compiles successfully
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-bugfixes-and-landing-page/03.1-02-SUMMARY.md`
</output>
