---
phase: 03.1-bugfixes-and-landing-page
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/page.tsx
  - apps/web/components/landing/LandingNav.tsx
  - apps/web/components/landing/FeaturesSection.tsx
  - apps/web/components/landing/PricingSection.tsx
  - apps/web/components/landing/AboutSection.tsx
autonomous: true

must_haves:
  truths:
    - "Landing page has navigation tabs (Features, Pricing, About)"
    - "User can click tabs to scroll to respective sections"
    - "User can navigate to login/register from landing page"
    - "Landing page maintains cosmic glassmorphism theme"
    - "All landing page text is in Bulgarian Cyrillic"
  artifacts:
    - path: "apps/web/app/page.tsx"
      provides: "Landing page with sections and navigation"
      min_lines: 50
    - path: "apps/web/components/landing/LandingNav.tsx"
      provides: "Navigation component with tabs"
      contains: "Функции"
    - path: "apps/web/components/landing/FeaturesSection.tsx"
      provides: "Features section content"
      contains: "id=\"features\""
    - path: "apps/web/components/landing/PricingSection.tsx"
      provides: "Pricing comparison section"
      contains: "id=\"pricing\""
  key_links:
    - from: "apps/web/components/landing/LandingNav.tsx"
      to: "#features, #pricing, #about sections"
      via: "anchor links with smooth scroll"
      pattern: "href=\"#"
    - from: "apps/web/app/page.tsx"
      to: "LandingNav component"
      via: "import and render"
      pattern: "LandingNav"
---

<objective>
Create a proper landing page with navigation tabs for Features, Pricing, and About sections.

Purpose: FEAT-01 requires a landing page with navigation tabs. The current landing page is basic with just feature cards. Need to add proper navigation, section anchors, and structure for a marketing-ready landing page.

Output: Landing page with sticky navigation, smooth-scroll anchor links to Features/Pricing/About sections, and login/register CTA.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/web/app/page.tsx
@packages/ui/src/components/GlassCard.tsx
@packages/ui/src/components/Text.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create landing page navigation component</name>
  <files>apps/web/components/landing/LandingNav.tsx</files>
  <action>
Create a sticky navigation component for the landing page with:
1. Logo/brand on the left
2. Navigation tabs in center: Функции (Features), Цени (Pricing), За нас (About)
3. Login/Register CTA on the right

Structure:
```tsx
'use client'

import Link from 'next/link'

export function LandingNav() {
  const scrollTo = (id: string) => {
    const element = document.getElementById(id)
    if (element) {
      element.scrollIntoView({ behavior: 'smooth' })
    }
  }

  return (
    <nav className="sticky top-0 z-50 border-b border-slate-800/50 bg-slate-900/80 backdrop-blur-xl">
      <div className="container mx-auto flex h-16 items-center justify-between px-4">
        {/* Logo */}
        <div className="flex items-center gap-3">
          <div className="h-8 w-8 rounded-lg bg-gradient-to-br from-purple-500 to-violet-600 flex items-center justify-center">
            <span className="text-white font-bold text-sm">C</span>
          </div>
          <span className="font-semibold text-slate-100">Celestia</span>
        </div>

        {/* Nav tabs - hidden on mobile, visible on md+ */}
        <div className="hidden md:flex items-center gap-6">
          <button
            onClick={() => scrollTo('features')}
            className="text-slate-300 hover:text-white transition-colors"
          >
            Функции
          </button>
          <button
            onClick={() => scrollTo('pricing')}
            className="text-slate-300 hover:text-white transition-colors"
          >
            Цени
          </button>
          <button
            onClick={() => scrollTo('about')}
            className="text-slate-300 hover:text-white transition-colors"
          >
            За нас
          </button>
        </div>

        {/* CTA */}
        <div className="flex items-center gap-3">
          <Link
            href="/sign-in"
            className="text-slate-300 hover:text-white transition-colors text-sm"
          >
            Вход
          </Link>
          <Link
            href="/sign-up"
            className="rounded-lg bg-gradient-to-r from-purple-600 to-violet-600 px-4 py-2 text-sm font-medium text-white transition-all hover:from-purple-500 hover:to-violet-500"
          >
            Регистрация
          </Link>
        </div>
      </div>
    </nav>
  )
}
```

Style to match the cosmic glassmorphism theme.
  </action>
  <verify>
File exists and contains navigation with Bulgarian text for tabs.
  </verify>
  <done>
Landing navigation component exists with Функции/Цени/За нас tabs and login/register CTAs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create landing page sections</name>
  <files>
    apps/web/components/landing/FeaturesSection.tsx
    apps/web/components/landing/PricingSection.tsx
    apps/web/components/landing/AboutSection.tsx
  </files>
  <action>
Create three section components with proper IDs for anchor navigation:

**FeaturesSection.tsx:**
- id="features"
- Showcase 3 main features: Натална карта, Дневен хороскоп, AI Оракул
- Use GlassCard components with icons
- All text in Bulgarian

**PricingSection.tsx:**
- id="pricing"
- Two pricing tiers: Безплатен (Free) and Премиум (Premium)
- Free: Basic features list
- Premium: All features + price placeholder (e.g., "9.99 лв/месец")
- Use comparison cards
- CTA buttons for each tier

**AboutSection.tsx:**
- id="about"
- Brief about text for Celestia AI
- Mission statement about personalized astrology
- All in Bulgarian Cyrillic

Each section should:
- Have proper id attribute for scroll targeting
- Use cosmic glassmorphism styling
- Be fully responsive (mobile/desktop)
- All text in Bulgarian
  </action>
  <verify>
Each file exists and contains the expected id attribute and Bulgarian content.
  </verify>
  <done>
Three landing sections exist with proper IDs and Bulgarian content.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update landing page to use new components</name>
  <files>apps/web/app/page.tsx</files>
  <action>
Refactor the landing page to:
1. Import and use LandingNav at the top
2. Add hero section with CTA
3. Import and use FeaturesSection
4. Import and use PricingSection
5. Import and use AboutSection
6. Keep footer

Structure:
```tsx
import { LandingNav } from '@/components/landing/LandingNav'
import { FeaturesSection } from '@/components/landing/FeaturesSection'
import { PricingSection } from '@/components/landing/PricingSection'
import { AboutSection } from '@/components/landing/AboutSection'
import Link from 'next/link'

export default function HomePage() {
  return (
    <div className="min-h-screen bg-gradient-to-b from-slate-900 via-slate-900 to-purple-900/20">
      <LandingNav />

      {/* Hero section */}
      <section className="container mx-auto px-4 py-20 text-center">
        <h1 className="text-4xl md:text-6xl font-bold mb-6 bg-gradient-to-r from-purple-400 to-violet-400 bg-clip-text text-transparent">
          Celestia AI
        </h1>
        <p className="text-xl text-slate-300 mb-8 max-w-2xl mx-auto">
          Вашият персонален астрологичен спътник. Открийте тайните на звездите.
        </p>
        <Link
          href="/sign-up"
          className="inline-flex rounded-lg bg-gradient-to-r from-purple-600 to-violet-600 px-8 py-4 text-lg font-medium text-white transition-all hover:from-purple-500 hover:to-violet-500"
        >
          Започнете безплатно
        </Link>
      </section>

      <FeaturesSection />
      <PricingSection />
      <AboutSection />

      {/* Footer */}
      <footer className="border-t border-slate-800/50 py-8">
        <p className="text-center text-slate-500 text-sm">
          &copy; 2026 Celestia AI. Всички права запазени.
        </p>
      </footer>
    </div>
  )
}
```

Remove the old GlassCard imports since features will be in FeaturesSection.
  </action>
  <verify>
Run TypeScript check: `cd apps/web && npx tsc --noEmit`
Page imports and renders all new components.
  </verify>
  <done>
Landing page renders with navigation, hero, features, pricing, about sections, and footer.
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `cd apps/web && npx tsc --noEmit`
2. LandingNav exists with Bulgarian tab labels
3. All sections have proper IDs for anchor links
4. Landing page imports and renders all components
5. All text is in Bulgarian Cyrillic
</verification>

<success_criteria>
- [ ] Landing page has sticky navigation with tabs
- [ ] Tabs link to Features, Pricing, About sections via smooth scroll
- [ ] Login and Register CTAs visible in navigation
- [ ] Features section showcases 3 main features
- [ ] Pricing section shows free vs premium comparison
- [ ] About section describes the app mission
- [ ] All text is in Bulgarian Cyrillic
- [ ] Responsive design works on mobile and desktop
- [ ] TypeScript compiles successfully
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-bugfixes-and-landing-page/03.1-03-SUMMARY.md`
</output>
