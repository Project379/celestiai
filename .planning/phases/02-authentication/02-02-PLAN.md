---
phase: 02-authentication
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - apps/web/app/(auth)/auth/[[...auth]]/page.tsx
  - apps/web/app/(auth)/layout.tsx
  - apps/web/components/auth/AuthBackground.tsx
  - apps/web/components/auth/AuthHeader.tsx
autonomous: true

must_haves:
  truths:
    - "User can access combined sign-in/sign-up page at /auth"
    - "Auth page displays Celestia branding with cosmic background"
    - "Clerk components render in Bulgarian language"
    - "Auth page matches cosmic glassmorphism theme"
  artifacts:
    - path: "apps/web/app/(auth)/auth/[[...auth]]/page.tsx"
      provides: "Combined auth page with SignIn component"
      contains: "SignIn"
    - path: "apps/web/app/(auth)/layout.tsx"
      provides: "Auth route group layout"
      min_lines: 10
    - path: "apps/web/components/auth/AuthBackground.tsx"
      provides: "Cosmic stars background component"
      contains: "stars"
    - path: "apps/web/components/auth/AuthHeader.tsx"
      provides: "Logo and tagline component"
      contains: "Celestia"
  key_links:
    - from: "apps/web/app/(auth)/auth/[[...auth]]/page.tsx"
      to: "@clerk/nextjs"
      via: "SignIn component import"
      pattern: "SignIn.*@clerk/nextjs"
    - from: "apps/web/app/(auth)/auth/[[...auth]]/page.tsx"
      to: "apps/web/components/auth/AuthBackground.tsx"
      via: "component import"
      pattern: "AuthBackground"
---

<objective>
Create the combined sign-in/sign-up authentication page at /auth with cosmic background, Celestia branding, and full Bulgarian localization using Clerk's prebuilt components.

Purpose: Deliver the primary auth UI that users interact with for registration (AUTH-01), login (AUTH-03), email verification (AUTH-02), and password reset (AUTH-04). The immersive cosmic design reinforces brand identity.

Output: Working /auth page with sign-in/sign-up tabs, cosmic background animation, Celestia logo, and Bulgarian UI text.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-authentication/02-RESEARCH.md
@.planning/phases/02-authentication/02-CONTEXT.md
@.planning/phases/02-authentication/02-01-SUMMARY.md
@apps/web/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create auth route group with layout</name>
  <files>
    - apps/web/app/(auth)/layout.tsx
  </files>
  <action>
Create the (auth) route group with a dedicated layout for authentication pages.

The auth layout should:
1. Be a minimal wrapper that doesn't duplicate the root layout
2. Center content vertically and horizontally on the page
3. Use full viewport height (min-h-screen)
4. Apply flex layout for centering
5. Optionally add padding for mobile responsiveness

Structure:
```
apps/web/app/
  (auth)/
    layout.tsx       <- Create this
    auth/
      [[...auth]]/
        page.tsx     <- Create in Task 2
```

The layout wraps auth pages but inherits ClerkProvider from root layout.
  </action>
  <verify>
- File exists at `apps/web/app/(auth)/layout.tsx`
- Exports a default function component
- Uses flex + items-center + justify-center for centering
- Uses min-h-screen for full height
- `pnpm typecheck` passes
  </verify>
  <done>
Auth route group layout created with centered, full-height container.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create cosmic background and branding components</name>
  <files>
    - apps/web/components/auth/AuthBackground.tsx
    - apps/web/components/auth/AuthHeader.tsx
  </files>
  <action>
Create two components for the auth page visual design:

**AuthBackground.tsx:**
A cosmic stars background component that creates the immersive space feel.
- Use absolute positioning to cover the entire viewport
- Create animated stars using CSS (small dots with twinkling animation)
- Use dark purple/blue gradient as base (#0F172A to #1E1B4B)
- Add subtle nebula/glow effects using radial gradients
- Use Tailwind animations or CSS keyframes
- Make it a client component ('use client') for animations
- Should NOT block interaction (pointer-events-none on the background layer)

**AuthHeader.tsx:**
Logo and tagline component for above the auth form.
- Display "Celestia AI" text or logo placeholder
- Bulgarian tagline underneath: "Вашият астрологичен спътник"
- Use cosmic purple gradient on the logo text
- Center-aligned
- Add subtle glow effect on logo

Both components should be reusable and match the cosmic glassmorphism theme established in Phase 1.
  </action>
  <verify>
- Both files exist in `apps/web/components/auth/`
- AuthBackground uses 'use client' directive
- AuthBackground has pointer-events-none to not block clicks
- AuthHeader displays "Celestia" and Bulgarian tagline
- `pnpm typecheck` passes
  </verify>
  <done>
Cosmic background with animated stars and branded header components created.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create combined auth page with Clerk SignIn</name>
  <files>
    - apps/web/app/(auth)/auth/[[...auth]]/page.tsx
  </files>
  <action>
Create the main authentication page using Clerk's SignIn component.

The page should:
1. Import `SignIn` from `@clerk/nextjs`
2. Import AuthBackground and AuthHeader components
3. Use the catch-all route pattern `[[...auth]]` to handle all Clerk auth flows

Page structure:
- AuthBackground as fixed backdrop
- Main container with relative positioning (above background)
- AuthHeader at top (logo + tagline)
- SignIn component with custom appearance overrides

SignIn configuration:
- Use `signUpUrl="/auth"` and `signInUrl="/auth"` for combined experience
- Clerk handles sign-up/sign-in tabs automatically
- Clerk handles forgot password flow (AUTH-04)
- Clerk handles email verification (AUTH-02)

Appearance overrides (complement ClerkProvider defaults):
- Card background: glassmorphism effect (rgba + backdrop-blur)
- Border: subtle slate-700 border
- Form elements match cosmic theme

The Bulgarian localization is already configured in ClerkProvider, so all text will automatically be in Bulgarian.

Note: This is a server component by default. Clerk's SignIn works in server components.
  </action>
  <verify>
- File exists at correct catch-all route path
- Imports SignIn from @clerk/nextjs
- Imports and renders AuthBackground and AuthHeader
- `pnpm typecheck` passes
- `pnpm build` succeeds
- Visiting /auth shows the Clerk sign-in UI
  </verify>
  <done>
Combined auth page created with Clerk SignIn, cosmic background, and Celestia branding.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `pnpm typecheck` passes
2. `pnpm build` succeeds
3. `pnpm dev` and navigate to http://localhost:3000/auth
4. Verify:
   - Cosmic stars background is visible and animated
   - Celestia AI logo/title appears above form
   - Bulgarian tagline is displayed
   - Clerk sign-in form renders with Bulgarian text
   - Tabs for sign-in/sign-up are functional
   - Forgot password link is present and works
   - Form has glassmorphism styling

Manual check:
```bash
cd C:/Users/ntone/Desktop/sub-project
pnpm typecheck
pnpm --filter web build
pnpm dev
# Then visit http://localhost:3000/auth in browser
```
</verification>

<success_criteria>
- AUTH-01: Sign-up form visible and functional
- AUTH-02: Email verification flow triggered after sign-up (Clerk handles)
- AUTH-03: Sign-in form visible and functional
- AUTH-04: Forgot password link present and works (Clerk handles)
- Cosmic background with animated stars
- Celestia branding with Bulgarian tagline
- All Clerk UI text in Bulgarian
- Glassmorphism styling matches cosmic theme
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication/02-02-SUMMARY.md`
</output>
