---
phase: 02-authentication
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - apps/web/middleware.ts
  - apps/web/components/auth/UserMenu.tsx
  - apps/web/components/auth/SessionExpiryModal.tsx
  - apps/web/components/auth/ProtectedTeaser.tsx
  - apps/web/components/auth/LogoutConfirmDialog.tsx
  - apps/web/app/(protected)/layout.tsx
  - apps/web/app/(protected)/dashboard/page.tsx
  - apps/web/app/api/user/route.ts
autonomous: true

must_haves:
  truths:
    - "Protected routes redirect unauthenticated users appropriately"
    - "User can log out with confirmation dialog"
    - "API routes validate authentication before processing"
    - "Session expiry shows modal instead of hard redirect"
    - "Protected pages show teaser with CTA for unauthenticated users"
  artifacts:
    - path: "apps/web/middleware.ts"
      provides: "Route protection configuration"
      contains: "isProtectedRoute"
    - path: "apps/web/components/auth/UserMenu.tsx"
      provides: "User avatar dropdown with logout"
      contains: "UserButton"
    - path: "apps/web/components/auth/LogoutConfirmDialog.tsx"
      provides: "Logout confirmation modal"
      contains: "signOut"
    - path: "apps/web/components/auth/SessionExpiryModal.tsx"
      provides: "Session expiry notification"
      contains: "isSignedIn"
    - path: "apps/web/components/auth/ProtectedTeaser.tsx"
      provides: "Teaser overlay for protected content"
      contains: "SignInButton"
    - path: "apps/web/app/(protected)/dashboard/page.tsx"
      provides: "Protected dashboard page"
      min_lines: 20
    - path: "apps/web/app/api/user/route.ts"
      provides: "Protected API route example"
      contains: "auth.protect"
  key_links:
    - from: "apps/web/components/auth/UserMenu.tsx"
      to: "@clerk/nextjs"
      via: "UserButton import"
      pattern: "UserButton"
    - from: "apps/web/components/auth/LogoutConfirmDialog.tsx"
      to: "@clerk/nextjs"
      via: "useClerk hook"
      pattern: "useClerk"
    - from: "apps/web/app/api/user/route.ts"
      to: "@clerk/nextjs/server"
      via: "auth import"
      pattern: "auth\\.protect"
---

<objective>
Implement protected route handling with teaser pattern, user menu with logout confirmation, session expiry modal, and API route protection. This completes the auth flow with proper session management.

Purpose: Ensure authenticated-only content is protected (SEC-11, SEC-17), users can log out securely (AUTH-06), and session handling follows the UX decisions from CONTEXT.md (teaser + overlay, soft expiry modal).

Output: Working protected routes, user menu with logout, session expiry handling, and protected API route pattern.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-authentication/02-RESEARCH.md
@.planning/phases/02-authentication/02-CONTEXT.md
@.planning/phases/02-authentication/02-01-SUMMARY.md
@apps/web/middleware.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure protected routes in middleware and create protected layout</name>
  <files>
    - apps/web/middleware.ts
    - apps/web/app/(protected)/layout.tsx
  </files>
  <action>
Update middleware route matchers and create protected route group.

**Middleware updates:**
Ensure the middleware (created in 02-01) has proper route matchers:
- Public routes: `/`, `/auth(.*)`, `/api/webhooks(.*)`
- Protected routes: `/dashboard(.*)`, `/settings(.*)`, `/chart(.*)`

If the middleware from 02-01 doesn't have these matchers complete, add them now using `createRouteMatcher`:
```typescript
const isPublicRoute = createRouteMatcher([
  '/',
  '/auth(.*)',
  '/api/webhooks(.*)',
])

const isProtectedRoute = createRouteMatcher([
  '/dashboard(.*)',
  '/settings(.*)',
  '/chart(.*)',
])
```

In the middleware function, call `await auth.protect()` when `isProtectedRoute(request)` is true.

**Protected layout:**
Create `apps/web/app/(protected)/layout.tsx`:
- Server component that checks auth status
- Import `auth` from `@clerk/nextjs/server`
- The layout can be minimal since middleware handles protection
- Add a header area for UserMenu (will be added in Task 2)
- Use consistent styling with app theme

The protected layout provides a shell for authenticated pages with common elements like navigation and user menu.
  </action>
  <verify>
- Middleware has isPublicRoute and isProtectedRoute matchers
- Middleware calls auth.protect() for protected routes
- Protected layout exists at `apps/web/app/(protected)/layout.tsx`
- `pnpm typecheck` passes
  </verify>
  <done>
Protected routes configured in middleware, protected layout created with auth check.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create user menu with logout confirmation</name>
  <files>
    - apps/web/components/auth/UserMenu.tsx
    - apps/web/components/auth/LogoutConfirmDialog.tsx
  </files>
  <action>
Create user menu component with Clerk's UserButton and custom logout confirmation.

**UserMenu.tsx:**
- Client component ('use client')
- Import `UserButton` from `@clerk/nextjs`
- Wrap UserButton in a container for positioning
- Style with cosmic theme (purple ring, slate background)
- Use appearance prop to customize popover styling

**LogoutConfirmDialog.tsx:**
- Client component ('use client')
- Use `useClerk` hook to access `signOut` function
- Create a modal/dialog component (can use native dialog or simple div overlay)
- Bulgarian text: "Сигурни ли сте, че искате да излезете?" (Are you sure you want to log out?)
- Two buttons: "Отказ" (Cancel) and "Изход" (Log out)
- Style with glassmorphism (backdrop-blur, semi-transparent background)
- On confirm, call `signOut({ redirectUrl: '/' })`

The UserMenu should show the avatar and when clicked, show a dropdown. The dropdown should have a custom "Log out" option that triggers the LogoutConfirmDialog instead of immediately signing out.

However, since customizing UserButton's menu items is limited, an alternative approach:
- Use UserButton normally but intercept sign-out via UserButton's `signOutCallback` prop (if available)
- OR create a completely custom user dropdown using `useUser()` hook and custom UI

Recommended: Check if UserButton supports `afterSignOutUrl` and custom sign-out handling. If not, create a custom dropdown with user avatar that includes the logout confirmation flow.
  </action>
  <verify>
- UserMenu.tsx exists and renders user avatar/button
- LogoutConfirmDialog.tsx exists with Bulgarian text
- Dialog has Cancel and Log out buttons
- `pnpm typecheck` passes
  </verify>
  <done>
User menu component created with logout confirmation dialog following CONTEXT.md decision.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create session expiry modal and protected teaser components</name>
  <files>
    - apps/web/components/auth/SessionExpiryModal.tsx
    - apps/web/components/auth/ProtectedTeaser.tsx
  </files>
  <action>
Create session handling and teaser components per CONTEXT.md decisions.

**SessionExpiryModal.tsx:**
- Client component ('use client')
- Use `useAuth` hook from `@clerk/nextjs`
- Track if user was previously signed in
- When session expires (wasSignedIn && !isSignedIn), show modal instead of redirect
- Modal content:
  - Bulgarian text: "Сесията ви изтече" (Your session has expired)
  - Subtext: "Влезте отново, за да продължите" (Sign in again to continue)
  - Sign in button using `SignInButton` from Clerk with `mode="modal"`
- Style with glassmorphism overlay
- This preserves page context (user stays on current page)

Reference research document "Session Expiry Modal Pattern" for implementation.

**ProtectedTeaser.tsx:**
- Server OR client component (depending on usage)
- Accepts children (the actual content to tease)
- Shows blurred version of children with overlay
- Overlay contains:
  - Bulgarian CTA text: "Влезте, за да видите съдържанието" (Sign in to view content)
  - `SignInButton` with `mode="modal"` for quick sign-in
- Uses blur-sm and pointer-events-none on children
- Overlay is semi-transparent with centered CTA

This component wraps protected content and shows a teaser with sign-in prompt for unauthenticated users.
  </action>
  <verify>
- SessionExpiryModal.tsx tracks auth state and shows modal on expiry
- ProtectedTeaser.tsx renders blurred children with sign-in CTA
- Both have Bulgarian text
- `pnpm typecheck` passes
  </verify>
  <done>
Session expiry modal and protected teaser components created for soft auth UX.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create dashboard page and protected API route</name>
  <files>
    - apps/web/app/(protected)/dashboard/page.tsx
    - apps/web/app/api/user/route.ts
  </files>
  <action>
Create example protected page and API route demonstrating auth patterns.

**Dashboard page (apps/web/app/(protected)/dashboard/page.tsx):**
- Server component
- Import `auth` from `@clerk/nextjs/server`
- Get userId from `await auth()`
- Display welcome message with userId (or user details)
- Include UserMenu component in header area
- Include SessionExpiryModal component
- Simple placeholder content for now (will be built out in later phases)
- Bulgarian text: "Добре дошли!" (Welcome!), "Вашето табло" (Your dashboard)

**Protected API route (apps/web/app/api/user/route.ts):**
- Import `auth` from `@clerk/nextjs/server`
- GET handler that:
  1. Calls `await auth.protect()` first (returns 404 for unauthenticated)
  2. Gets userId from `await auth()`
  3. Returns JSON with userId
- This demonstrates SEC-17 (API routes validate authentication)

```typescript
import { auth } from '@clerk/nextjs/server'

export async function GET() {
  await auth.protect()
  const { userId } = await auth()
  return Response.json({ userId })
}
```

The dashboard page serves as:
1. Target redirect after sign-in
2. Example of protected page pattern
3. Place to include session monitoring
  </action>
  <verify>
- Dashboard page exists and displays user info
- Dashboard includes UserMenu and SessionExpiryModal
- API route exists at /api/user
- API route calls auth.protect() before processing
- `pnpm typecheck` passes
- `pnpm build` succeeds
- Unauthenticated request to /api/user returns 404
- Authenticated request to /api/user returns userId JSON
  </verify>
  <done>
Protected dashboard page and API route created demonstrating auth protection patterns.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `pnpm typecheck` passes
2. `pnpm build` succeeds
3. Manual testing with `pnpm dev`:

**Protected routes test:**
- Navigate to /dashboard while logged out -> redirected to /auth or shown teaser
- Log in -> can access /dashboard
- Dashboard shows welcome message and UserMenu

**Logout test:**
- Click user avatar/menu
- Click logout -> confirmation dialog appears
- Confirm logout -> signed out and redirected to /

**API protection test:**
```bash
curl http://localhost:3000/api/user
# Should return 404 (unauthenticated)

# While logged in (use browser network tab):
# GET /api/user should return { "userId": "user_xxx" }
```

**Session expiry test:**
- Sign in
- Clear cookies or wait for session to expire
- SessionExpiryModal should appear instead of hard redirect
</verification>

<success_criteria>
- AUTH-05: Session persists across browser refresh (Clerk default behavior)
- AUTH-06: User can log out with confirmation dialog
- SEC-11: Protected routes use auth.protect() middleware
- SEC-17: API routes validate authentication before processing
- Session expiry shows soft modal per CONTEXT.md
- Protected content shows teaser with CTA for unauthenticated users
- All Bulgarian UI text in auth components
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication/02-03-SUMMARY.md`
</output>
