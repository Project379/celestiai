# Competitor UX/Visual Teardown vs. Stellaeum MVP

**Retired 2026-08-13 — superseded by CLAUDE.md as the sole current stack/architecture reference.**

This document's chart-rendering claims (React Native Skia, a `NatalWheelSkia.tsx` component) no longer match the codebase and are not being corrected in place — CLAUDE.md at the repo root is now the single source of truth for tech stack and architecture. The competitor UX research itself (non-technical observations about other astrology apps) may still hold value; it just isn't separated from the stale technical framing here, so treat the whole document as unverified rather than picking out individual claims.

Original content (a competitor UX/visual teardown against the v1.0 feature requirements) is preserved in git history if historical context is ever needed — see `git log -- .planning/research/COMPETITOR_UX_VISUALS.md`.
